# 成功奖励调整总结

## 一、调整概述

根据实际训练情况，调整了成功奖励的计算方式：
1. **降低少量碰撞时的奖励**：50次碰撞时的比例从0.982降低到约0.946
2. **添加各智能体碰撞占比可视化**：使用堆叠面积图显示每个智能体的碰撞贡献

---

## 二、无碰撞比例计算调整

### 2.1 原计算方式

**少量碰撞时（碰撞比例 < 0.1）**：
- 无碰撞比例 = 1 - 碰撞比例（线性）
- 例如：50次碰撞(0.018) → 比例 = 0.982

**问题**：从实际训练来看，基本没有到达一半以上的，但50次碰撞时给的奖励（98%）还是太多。

### 2.2 新计算方式

**少量碰撞时（碰撞比例 < 0.1）**：
- 无碰撞比例 = (1 - 碰撞比例)^2.5（平方惩罚）
- 例如：50次碰撞(0.018) → 比例 = (0.982)^2.5 ≈ 0.946

**效果**：
- 50次碰撞时的比例从0.982降低到0.946（降低约3.7%）
- 更严厉地惩罚少量碰撞，避免给太多奖励

### 2.3 完整映射规则

**区域1：少量碰撞（碰撞比例 < 0.1）**：
```
无碰撞比例 = (1 - 碰撞比例)^2.5
```

**区域2：中等碰撞（0.1 <= 碰撞比例 < 0.5）**：
```
无碰撞比例 = 1 - 碰撞比例（线性）
```

**区域3：大量碰撞（碰撞比例 >= 0.5）**：
```
无碰撞比例 = 0.2 × (1 - (碰撞比例 - 0.5) / 0.5)
```

---

## 三、各智能体碰撞占比可视化

### 3.1 数据记录

**新增数据结构**：
- `maddpg._episode_agent_collision_counts`：每回合每个智能体的碰撞次数
- 格式：`[[agent0_collisions, agent1_collisions, ...], ...]`

**记录位置**：`paper3d_train_optimized.py:14917-15000`

### 3.2 可视化方式

**堆叠面积图（Stacked Area Chart）**：
- X轴：回合数
- Y轴：碰撞次数（堆叠显示）
- 每个智能体一个颜色区域，显示其碰撞贡献

**优势**：
- 可以直观看到每个智能体的碰撞占比
- 可以快速识别哪个智能体碰撞最多
- 总碰撞次数 = 所有智能体碰撞次数的和

### 3.3 代码实现

**绘图位置**：`paper3d_train_optimized.py:16546-16590`

**关键代码**：
```python
# 准备堆叠数据：每个智能体一个数组
agent_collision_arrays = []
for agent_idx in range(n_agents):
    agent_collisions = []
    for ep_idx in range(plot_len):
        if ep_idx < len(agent_collision_counts):
            ep_data = agent_collision_counts[ep_idx]
            if isinstance(ep_data, list) and agent_idx < len(ep_data):
                agent_collisions.append(int(ep_data[agent_idx]))
            else:
                agent_collisions.append(0)
        else:
            agent_collisions.append(0)
    agent_collision_arrays.append(agent_collisions)

# 使用堆叠面积图
axes[1].stackplot(plot_episodes, *agent_collision_arrays, 
                 labels=[f'Agent {i}' for i in range(n_agents)],
                 colors=colors[:n_agents], alpha=0.7)
```

---

## 四、计算示例

### 4.1 配置参数

- `SUCCESS_REWARD_VALUE = 3000.0`（基础值）
- `SUCCESS_WEIGHT = 2.0`（权重）
- `EPISODE_LENGTH = 2800`（回合总步数）

### 4.2 场景1：没有碰撞

**总碰撞次数**：0次

**无碰撞比例**：
- 碰撞比例 = 0 / 2800 = 0.0
- 无碰撞比例 = 1.0

**成功奖励**：
- 基础值：3000.0
- 缩放后：3000.0 × 1.0 = 3000.0
- 应用权重后：3000.0 × 2.0 = **6,000**

### 4.3 场景2：只碰了几次（50次）

**总碰撞次数**：50次

**无碰撞比例**：
- 碰撞比例 = 50 / 2800 = 0.018
- 由于碰撞比例 < 0.1，使用平方惩罚
- 无碰撞比例 = (1 - 0.018)^2.5 = 0.982^2.5 ≈ **0.946**

**成功奖励**：
- 基础值：3000.0
- 缩放后：3000.0 × 0.946 = 2838.0
- 应用权重后：2838.0 × 2.0 = **5,676**

**对比原方式**：
- 原方式：3000.0 × 0.982 × 2.0 = 5,892
- 新方式：3000.0 × 0.946 × 2.0 = 5,676
- **降低约3.7%**

### 4.4 场景3：碰了一半（1400次）

**总碰撞次数**：1400次

**无碰撞比例**：
- 碰撞比例 = 1400 / 2800 = 0.5
- 由于碰撞比例 >= 0.5，使用非线性映射
- 无碰撞比例 = 0.2 × (1 - (0.5 - 0.5) / 0.5) = **0.2**

**成功奖励**：
- 基础值：3000.0
- 缩放后：3000.0 × 0.2 = 600.0
- 应用权重后：600.0 × 2.0 = **1,200**

### 4.5 场景4：碰了全部（2800次）

**总碰撞次数**：2800次

**无碰撞比例**：
- 碰撞比例 = 2800 / 2800 = 1.0
- 由于碰撞比例 >= 0.5，使用非线性映射
- 无碰撞比例 = 0.2 × (1 - (1.0 - 0.5) / 0.5) = 0.0

**成功奖励**：
- 基础值：3000.0
- 缩放后：3000.0 × 0.0 = 0.0
- 应用权重后：0.0 × 2.0 = **0**

---

## 五、可视化效果

### 5.1 原可视化

**单线图**：
- 只显示总碰撞次数
- 无法区分各智能体的贡献

### 5.2 新可视化

**堆叠面积图**：
- 显示每个智能体的碰撞次数（不同颜色）
- 可以直观看到各智能体的占比
- 总高度 = 所有智能体碰撞次数的和

**示例**：
- Agent 0（红色区域）：500次碰撞
- Agent 1（青色区域）：300次碰撞
- Agent 2（蓝色区域）：200次碰撞
- 总碰撞次数：1000次

---

## 六、总结

### 6.1 修改完成

✅ 降低少量碰撞时的奖励（使用平方惩罚）
✅ 添加各智能体碰撞占比可视化（堆叠面积图）
✅ 记录每个智能体的碰撞次数

### 6.2 预期效果

1. **更严厉的惩罚**：少量碰撞时的奖励降低约3.7%，避免给太多
2. **更好的可视化**：可以直观看到每个智能体的碰撞贡献
3. **更容易诊断**：可以快速识别哪个智能体碰撞最多

### 6.3 下一步

1. 运行训练，观察成功奖励的变化
2. 检查堆叠面积图，确认各智能体的碰撞占比显示正确
3. 根据训练效果，进一步调整平方惩罚的指数（当前为2.5）
