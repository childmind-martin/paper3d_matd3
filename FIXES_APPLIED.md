# 已应用的修复

## 一、Shell脚本错误修复 ✅

**文件**: `run_optimized.sh:1143`
**问题**: 注释行中的括号被shell误解析
**修复**: 移除注释中的括号，使用普通文本

## 二、PF力归一化过度压缩修复 ✅

**文件**: `paper3d_train_optimized.py:5241` (MADDPG版本)
**文件**: `paper3d_train_optimized.py:11237` (MATD3版本)

**问题**: 
```python
# 修复前
norm_base = tf.maximum(norm_base, tf.cast(self.c_max_force * 1.0, dtype_action))
# 如果 c_max_force = 8.0，norm_base最小值 = 8.0
# 如果实际PF力 mag_pf_raw = 2.0，归一化后 mag_pf_norm = 2.0 / 8.0 = 0.25
# 即使force_ratio=0.65，PF修正影响 = 0.65 * 0.25 = 0.1625（只有16%）
```

**修复**:
```python
# 修复后
norm_base = tf.maximum(norm_base, tf.cast(self.c_max_force * 0.1, dtype_action))
# 如果 c_max_force = 8.0，norm_base最小值 = 0.8
# 如果实际PF力 mag_pf_raw = 2.0，归一化后 mag_pf_norm = 2.0 / 2.0 = 1.0（如果mag_pf_raw > 0.8）
# 如果实际PF力 mag_pf_raw = 0.5，归一化后 mag_pf_norm = 0.5 / 0.8 = 0.625
# 即使force_ratio=0.30，PF修正影响 = 0.30 * 0.625 = 0.1875（18.75%，比之前好）
```

**预期效果**:
- PF力归一化后幅值从0.19-0.46提升到0.625-1.0
- PF修正影响从9%提升到18-30%
- 轨迹变化更明显，Reward波动可能减小

## 三、根本原因总结

### 3.1 轨迹不变的根本原因

1. ✅ **PF修正影响太小**（修复前只有9%）
   - **问题**: 归一化基准最小值太大（`c_max_force * 1.0 = 8.0`）
   - **影响**: 实际PF力（2-5）归一化后只有0.25-0.625
   - **修复**: 降低归一化基准最小值到`c_max_force * 0.1 = 0.8`
   - **预期**: PF修正影响提升到18-30%

2. ⚠️ **Actor输出变化小**（需要进一步检查）
   - 可能原因: 学习率过低、梯度消失、局部最优
   - 建议: 如果修复PF归一化后仍无改善，检查Actor学习

3. ⚠️ **动作映射可能过度压缩**（需要检查）
   - 当前: `action_range=[1.0, 1.0, 1.0]`
   - 建议: 如果轨迹仍不变，考虑增大action_range

### 3.2 Reward波动大的根本原因

1. ✅ **碰撞惩罚占主导**
   - 权重: 3.0（最高）
   - 单次碰撞惩罚: -30.0 * 3.0 = -90.0
   - 如果每步碰撞: 2200步 × -90 = -198,000

2. ✅ **碰撞频率变化**
   - 碰撞检测基于距离`d_min_current`
   - 位置略有变化（例如高度变化）可能导致某些位置碰撞，某些位置不碰撞
   - **碰撞频率的微小变化导致Reward大幅波动**

3. ✅ **其他奖励分项变化小**
   - 如果轨迹不变，其他奖励分项也不变
   - 只有碰撞惩罚在波动

## 四、验证方法

### 4.1 验证PF力归一化修复

在训练日志中查找：
```bash
# 如果启用了PF调试输出
grep "PF力幅值\|mag_pf\|norm_base" logs/*/train.log
```

预期看到：
- `mag_pf_norm`从0.19-0.46提升到0.625-1.0
- PF修正影响从9%提升到18-30%

### 4.2 验证轨迹变化

观察训练日志中的轨迹图：
- 修复前: 轨迹基本不变
- 修复后: 轨迹应该有明显变化

### 4.3 验证Reward波动

观察训练日志中的Reward曲线：
- 修复前: Reward在-154k到70k之间大幅波动
- 修复后: Reward波动应该减小（因为轨迹变化，碰撞频率更稳定）

## 五、如果问题仍然存在

### 5.1 如果轨迹仍然不变

1. **检查Actor学习**
   - 检查Actor loss是否在下降
   - 检查Actor输出方差是否在增加
   - 如果Actor loss不下降，考虑提高学习率

2. **检查动作映射**
   - 检查`action_range`是否太小
   - 考虑增大`action_range`以增强动作差异

3. **检查PF力计算**
   - 检查目标位置提取是否正确
   - 检查PF参数映射是否正确

### 5.2 如果Reward仍然波动大

1. **降低碰撞惩罚权重**
   ```bash
   export COLLISION_WEIGHT=${COLLISION_WEIGHT:-2.0}  # 从3.0降低到2.0
   ```

2. **检查碰撞检测**
   - 检查碰撞阈值是否合理
   - 检查距离计算是否正确

3. **增加其他奖励分项权重**
   - 提高距离奖励权重
   - 提高接近奖励权重

