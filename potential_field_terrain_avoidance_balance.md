# 势场引导与地形避障平衡方案

## 一、问题分析

### 1.1 用户反馈

**问题**："因为有势场引导，智能体总是很容易到达目标点，但是难以躲避地形"

**核心矛盾**：
1. **目标吸引力过强**：让智能体容易到达目标 ✅
2. **地形排斥力不足**：难以躲避地形 ❌
3. **两者不平衡**：目标吸引力可能抵消地形排斥力

### 1.2 当前势场参数

**目标吸引力**：
- `GOAL_ATTRACTION = 8.0`（基准值）
- `LAMBDA_1_BASE = 8.5`（距离阈值d0）
- 公式：近距离常数，远距离线性增长

**地形排斥力**：
- `TERRAIN_REPULSION = 6000.0`（已经很高）
- `AGENT_INFLUENCE_RANGE = 150.0`（检测半径）
- 公式：`F_rep = k_rep × (1/r_min - 1/R_safe) × (1/r_min²) × κ × n̂`
- 目标距离影响因子：`κ = exp(-goal_dist / 50.0)`

**混合比例**：
- `ACTION_FORCE_RATIO = 0.50`（50%势场，50%网络动作）

### 1.3 问题根源

**目标吸引力过强**：
- 从任意距离都会产生吸引力
- 近距离时：常数吸引力 = 8.0 × 8.5 = 68.0
- 远距离时：线性增长吸引力 = 2 × 8.0 × dist

**地形排斥力可能不足**：
- 虽然`TERRAIN_REPULSION = 6000.0`很高，但公式中有多个衰减因子：
  - `(1/r_min - 1/R_safe)`：当r_min较大时，这个值很小
  - `(1/r_min²)`：距离的平方衰减
  - `κ = exp(-goal_dist / 50.0)`：距离目标越远，地形斥力越弱

**目标距离影响因子κ的问题**：
- `κ = exp(-goal_dist / 50.0)`
- 距离目标100米：κ = exp(-100/50) = exp(-2) ≈ 0.135（地形斥力衰减到13.5%）
- 距离目标50米：κ = exp(-50/50) = exp(-1) ≈ 0.368（地形斥力衰减到36.8%）
- **问题**：距离目标越远，地形斥力越弱，无法有效阻止贴地飞行

---

## 二、解决方案

### 2.1 方案1：增强地形排斥力（推荐）✅

**核心思想**：进一步提高地形排斥力基准值，确保即使在远距离也能有效避障

**修改**：
```bash
export TERRAIN_REPULSION=${TERRAIN_REPULSION:-8000.0}  # 从6000提高到8000
```

**效果**：
- 地形斥力强度增加33%
- 即使在远距离（κ=0.135），地形斥力仍然足够强
- 能够更好地对抗目标吸引力

### 2.2 方案2：调整目标距离影响因子κ（推荐）✅

**核心思想**：修改κ的计算方式，让地形斥力在远距离时也保持足够强度

**当前公式**：
```python
kappa = exp(-goal_dist / 50.0)  # 距离目标越远，地形斥力越弱
```

**问题**：
- 距离目标100米时，κ ≈ 0.135，地形斥力衰减到13.5%
- 这导致智能体在远距离时无法有效避障

**改进方案**：
```python
# 方案A：使用更慢的衰减
kappa = exp(-goal_dist / 100.0)  # 从50.0改为100.0，衰减更慢

# 方案B：使用分段函数
if goal_dist < 50.0:
    kappa = 1.0  # 近距离：全强度
elif goal_dist < 100.0:
    kappa = 0.5 + 0.5 * exp(-(goal_dist - 50.0) / 50.0)  # 中距离：逐渐衰减
else:
    kappa = 0.3  # 远距离：保持30%强度（而非13.5%）

# 方案C：使用平方根衰减（更温和）
kappa = 1.0 / (1.0 + goal_dist / 50.0)  # 距离100米时，κ ≈ 0.33（而非0.135）
```

**推荐**：方案C（平方根衰减），更温和且保持足够强度

### 2.3 方案3：降低目标吸引力（可选）

**核心思想**：降低目标吸引力，让地形排斥力更容易占主导

**修改**：
```bash
export GOAL_ATTRACTION=${GOAL_ATTRACTION:-5.0}  # 从8.0降低到5.0
```

**效果**：
- 目标吸引力降低37.5%
- 地形排斥力更容易占主导
- 但可能影响到达目标的能力

### 2.4 方案4：增强高度奖励（已实施）✅

**当前配置**：
- `HEIGHT_WEIGHT = 3.5`（已经较高）
- 理想高度：15-75米

**效果**：
- 鼓励智能体保持在理想高度
- 避免贴地飞行
- 与势场引导形成互补

### 2.5 方案5：增强净空奖励（已实施）✅

**当前配置**：
- `CLEARANCE_WEIGHT_NEAR = 8.0`（近距离时）
- 已改为基于距离变化的计算方式

**效果**：
- 距离增加时给奖励（鼓励避障）
- 距离减少时给负奖励（惩罚接近危险）
- 与势场引导形成互补

---

## 三、推荐的综合方案

### 3.1 立即实施（高优先级）

1. **增强地形排斥力**
   ```bash
   export TERRAIN_REPULSION=${TERRAIN_REPULSION:-8000.0}  # 从6000提高到8000
   ```

2. **调整目标距离影响因子κ**
   - 修改代码中的κ计算方式
   - 从`exp(-goal_dist / 50.0)`改为`1.0 / (1.0 + goal_dist / 50.0)`
   - 效果：距离100米时，κ从0.135提高到0.33（提高144%）

### 3.2 中期优化（中优先级）

3. **适度降低目标吸引力**
   ```bash
   export GOAL_ATTRACTION=${GOAL_ATTRACTION:-6.0}  # 从8.0降低到6.0
   ```
   - 降低25%，但仍保持足够的目标导向
   - 让地形排斥力更容易占主导

### 3.3 长期优化（低优先级）

4. **调整势场混合比例**
   - 在接近地形时，增加势场比例（让避障更优先）
   - 在远离地形时，减少势场比例（让网络动作更自由）

---

## 四、效果预期

### 4.1 增强地形排斥力（6000 → 8000）

**距离目标100米时**：
- 原方式：地形斥力 = 6000 × 0.135 = 810
- 新方式：地形斥力 = 8000 × 0.135 = 1080
- **提高33%**

**距离目标50米时**：
- 原方式：地形斥力 = 6000 × 0.368 = 2208
- 新方式：地形斥力 = 8000 × 0.368 = 2944
- **提高33%**

### 4.2 调整κ计算方式（exp衰减 → 平方根衰减）

**距离目标100米时**：
- 原方式：κ = 0.135，地形斥力 = 6000 × 0.135 = 810
- 新方式：κ = 0.33，地形斥力 = 6000 × 0.33 = 1980
- **提高144%**

**距离目标50米时**：
- 原方式：κ = 0.368，地形斥力 = 6000 × 0.368 = 2208
- 新方式：κ = 0.50，地形斥力 = 6000 × 0.50 = 3000
- **提高36%**

### 4.3 综合效果

**距离目标100米时**：
- 原方式：地形斥力 = 810
- 新方式（增强+调整κ）：地形斥力 = 8000 × 0.33 = 2640
- **提高226%**

**效果**：
- ✅ 远距离时地形斥力足够强，能够有效阻止贴地飞行
- ✅ 近距离时地形斥力更强，能够有效避障
- ✅ 目标吸引力仍然足够强，能够引导到达目标

---

## 五、实现建议

### 5.1 修改地形排斥力基准值

**文件**：`run_optimized.sh`
**行数**：679

```bash
export TERRAIN_REPULSION=${TERRAIN_REPULSION:-8000.0}  # 从6000提高到8000
```

### 5.2 修改目标距离影响因子κ

**文件**：`paper3d_train_optimized.py`
**函数**：`_calculate_terrain_forces_sphere_tf`
**位置**：计算κ的地方

**原代码**：
```python
kappa = exp(-goal_dist / 50.0)
```

**新代码**：
```python
# 使用平方根衰减，让远距离时地形斥力也保持足够强度
kappa = 1.0 / (1.0 + goal_dist / 50.0)
```

### 5.3 适度降低目标吸引力（可选）

**文件**：`run_optimized.sh`
**行数**：664

```bash
export GOAL_ATTRACTION=${GOAL_ATTRACTION:-6.0}  # 从8.0降低到6.0
```

---

## 六、总结

### 6.1 核心问题

**势场引导让智能体容易到达目标，但难以躲避地形**

**根本原因**：
1. 目标吸引力过强（8.0）
2. 地形排斥力在远距离时衰减过快（κ = exp(-goal_dist / 50.0)）
3. 两者不平衡，目标吸引力可能抵消地形排斥力

### 6.2 解决方案

**推荐方案**：
1. ✅ 增强地形排斥力：6000 → 8000（提高33%）
2. ✅ 调整κ计算方式：从exp衰减改为平方根衰减（远距离时提高144%）
3. ⚠️ 适度降低目标吸引力：8.0 → 6.0（降低25%，可选）

### 6.3 预期效果

1. **远距离时**：地形斥力从810提高到2640（提高226%），能够有效阻止贴地飞行
2. **近距离时**：地形斥力从2208提高到4000（提高81%），能够有效避障
3. **目标导向**：目标吸引力仍然足够强（6.0），能够引导到达目标

### 6.4 下一步

1. 修改`TERRAIN_REPULSION`从6000提高到8000
2. 修改κ计算方式从exp衰减改为平方根衰减
3. 测试效果，确认避障能力提升
