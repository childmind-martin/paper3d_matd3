# 净空奖励权重调整分析

## 一、当前碰撞情况分析

### 1.1 终端输出观察

**最近几个回合的碰撞情况**：
- **回合892**：7次碰撞（Agent2有7次，Agent0和Agent1无碰撞）
- **回合893**：97次碰撞（Agent0: 73次，Agent1: 16次，Agent2: 8次）
- **回合894**：37次碰撞（Agent0: 35次，Agent1: 2次，Agent2: 0次）
- **回合895**：172次碰撞（Agent0: 20次，Agent1: 0次，Agent2: 152次）

**观察**：
- ⚠️ 碰撞仍然存在，且波动较大（7-172次）
- ⚠️ 不同智能体的碰撞分布不均匀（某些智能体碰撞较多）
- ✅ 部分回合碰撞较少（如回合892只有7次）

### 1.2 碰撞特征

**模式分析**：
1. **接近目标时的碰撞**：智能体在接近目标时（<20m）更容易碰撞
2. **智能体差异**：不同智能体的避障能力差异较大
3. **回合间波动**：碰撞次数在回合间波动较大，说明策略不稳定

---

## 二、当前净空奖励配置

### 2.1 权重设置

**当前值**：
- `CLEARANCE_WEIGHT_NEAR = 6.5`（已从8.0降低18.75%）
- `CLEARANCE_WEIGHT_FAR = 0.2`（远距离权重）
- `CLEARANCE_PENALTY_WEIGHT = 4.0`（避障惩罚权重）

**计算方式**：
- ✅ 已改为基于距离变化（不是绝对距离）
- ✅ 距离增加时给正奖励（鼓励避障）
- ✅ 距离减少时给负奖励（惩罚接近危险）
- ✅ 距离不变时给零奖励（避免刷分）

### 2.2 相关参数

**势场参数**：
- `TERRAIN_REPULSION = 8000.0`（地形排斥力，已增强）
- `GOAL_ATTRACTION = 6.0`（目标吸引力，已降低）

**其他奖励**：
- `APPROACH_WEIGHT = 1.0`（接近奖励权重，已提高）
- `COLLISION_WEIGHT = 4.0`（碰撞惩罚权重，已加强）

---

## 三、是否需要进一步降低净空奖励权重？

### 3.1 支持降低的理由

1. **仍有碰撞**：
   - 虽然已降低到6.5，但仍有碰撞（7-172次）
   - 说明净空奖励权重可能仍然偏高

2. **接近目标时的权衡**：
   - 接近目标时（<20m），净空奖励权重6.5可能过于强势
   - 可能导致智能体在"避障"和"接近目标"之间难以平衡

3. **累积效应**：
   - 即使改为基于距离变化，近距离时6.5权重仍然较高
   - 如果智能体在接近目标时频繁调整方向避障，累积奖励仍然可观

### 3.2 反对降低的理由

1. **避障引导的重要性**：
   - 净空奖励是避障的主要引导信号
   - 如果降得太低，可能导致避障能力下降

2. **已改为基于距离变化**：
   - 计算方式已改进，不再是"保持安全距离但不接近目标"刷分
   - 应该先观察改进后的效果

3. **其他因素影响**：
   - 碰撞可能不完全由净空奖励权重决定
   - 还可能与势场参数、网络学习能力、探索策略等有关

### 3.3 折中方案

**建议**：适度降低，但不要降得太低

**推荐值**：
- `CLEARANCE_WEIGHT_NEAR`: 6.5 → **5.0-5.5**（降低15-23%）
- 保持 `CLEARANCE_PENALTY_WEIGHT = 4.0`（避障惩罚权重不变）

**理由**：
1. ✅ 适度降低，不会完全削弱避障引导
2. ✅ 与提高的接近奖励权重（1.0）形成更好的平衡
3. ✅ 保持避障惩罚权重不变，确保安全约束

---

## 四、其他可能的影响因素

### 4.1 势场参数

**当前设置**：
- `TERRAIN_REPULSION = 8000.0`（地形排斥力）
- `GOAL_ATTRACTION = 6.0`（目标吸引力）

**可能的问题**：
- 如果地形排斥力过强，可能导致智能体在接近目标时难以平衡
- 如果目标吸引力过弱，可能导致智能体不够积极接近目标

**建议**：
- 保持当前设置，先观察净空奖励权重调整后的效果

### 4.2 网络学习能力

**观察**：
- 最佳奖励回合（263）已经很久没有更新
- 自适应学习率在降低（Actor LR: 0.000063 → 0.000056）

**可能的问题**：
- 网络可能陷入局部最优
- 学习率可能过低，导致学习能力不足

**建议**：
- 继续观察自适应学习率调整的效果
- 如果学习率过低，可能需要调整 `ADAPTIVE_MIN_LR`

### 4.3 探索策略

**当前设置**：
- `RANDOM_ACTION_PROB_TRAINING = 0.10`（随机动作概率）
- `NOISE_RESTART_INTERVAL = 25`（噪声重启间隔）

**可能的问题**：
- 探索不足，导致策略陷入局部最优
- 噪声尺度可能不够

**建议**：
- 保持当前设置，先观察净空奖励权重调整后的效果

---

## 五、最终建议

### 5.1 推荐调整

**净空奖励权重**：
- `CLEARANCE_WEIGHT_NEAR`: 6.5 → **5.5**（降低15%）

**理由**：
1. ✅ 适度降低，不会完全削弱避障引导
2. ✅ 与提高的接近奖励权重（1.0）形成更好的平衡
3. ✅ 保持避障惩罚权重不变，确保安全约束
4. ✅ 如果效果不好，可以进一步调整到5.0

### 5.2 保持不变的参数

**避障惩罚权重**：
- `CLEARANCE_PENALTY_WEIGHT = 4.0`（保持不变）

**理由**：
- 这是安全约束，不应该降低
- 当距离<安全距离时，需要强惩罚来阻止接近危险

### 5.3 观察指标

**调整后需要观察**：
1. ✅ 碰撞次数是否减少
2. ✅ 是否仍然能够有效避障
3. ✅ 接近目标的能力是否提高
4. ✅ 奖励累积是否更平衡

---

## 六、总结

### 6.1 当前状态

- ⚠️ 仍有碰撞（7-172次，波动较大）
- ✅ 净空奖励已改为基于距离变化（避免刷分）
- ✅ 已降低净空奖励权重（8.0 → 6.5）
- ✅ 已提高接近奖励权重（0.8 → 1.0）

### 6.2 建议

**适度降低净空奖励权重**：
- `CLEARANCE_WEIGHT_NEAR`: 6.5 → **5.5**（降低15%）

**预期效果**：
- ✅ 减少接近目标时的碰撞
- ✅ 提高目标导向能力
- ✅ 保持避障引导（不会完全削弱）

**如果效果不好**：
- 可以进一步降低到5.0
- 或者调整势场参数（TERRAIN_REPULSION, GOAL_ATTRACTION）
