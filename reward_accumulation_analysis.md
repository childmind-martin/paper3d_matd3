# 奖励累积分析报告

## 一、问题概述

根据训练数据分析，奖励值很高（400万-540万），但成功率极低（0%），说明存在"刷分"行为。本报告详细分析哪些奖励项导致了大量累积。

---

## 二、奖励项分类

### 2.1 每步都计算的奖励项（会大量累积）

这些奖励项在**每个训练步**都会计算，如果每回合有2800步，这些奖励会累积2800次。

#### 1. **距离奖励 (distance)** ⚠️ 高累积风险
- **权重**: `DISTANCE_WEIGHT = 0.8`
- **计算方式**: `-(relative_dist - 1.0) * 3.0`
- **返回值范围**: 约 -3.0 到 +3.0（取决于相对距离）
- **每步累积**: 0.8 × 3.0 = **2.4**（最大值）
- **2800步累积**: 2.4 × 2800 = **6,720**（最大值）
- **问题**: 即使智能体不接近目标，只要保持一定距离就能获得持续奖励

#### 2. **接近目标奖励 (approach)** ⚠️⚠️ 极高累积风险
- **权重**: `APPROACH_WEIGHT = 0.8`
- **计算方式**: `dist_change * 0.5`（接近时）
- **返回值范围**: 每步约 0.0 到 +0.5（取决于距离变化）
- **每步累积**: 0.8 × 0.5 = **0.4**（接近时）
- **2800步累积**: 0.4 × 2800 = **1,120**（持续接近时）
- **问题**: 这是最危险的累积项！智能体可以通过"持续接近但不到达"来刷分

#### 3. **方向奖励 (direction)** ⚠️ 中等累积风险
- **权重**: `DIRECTION_WEIGHT = 0.3`
- **计算方式**: `speed * 2.0 + alignment_bonus`（速度>0.05时）
- **返回值范围**: 约 0.0 到 5.0（取决于速度和方向对齐）
- **每步累积**: 0.3 × 5.0 = **1.5**（最大值）
- **2800步累积**: 1.5 × 2800 = **4,200**（最大值）
- **问题**: 只要智能体在移动，就能获得持续奖励

#### 4. **高度奖励 (height)** ⚠️⚠️ 极高累积风险
- **权重**: `HEIGHT_WEIGHT = 3.5`
- **计算方式**: 
  - 理想高度内: `1.0`
  - 低于理想高度: `-0.5 到 -2.0`
  - 高于理想高度: `-0.3 到 -1.5`
- **返回值范围**: 约 -2.0 到 +1.0
- **每步累积**: 3.5 × 1.0 = **3.5**（理想高度时）
- **2800步累积**: 3.5 × 2800 = **9,800**（保持在理想高度时）
- **问题**: 权重最高！只要保持在理想高度就能获得大量奖励

#### 5. **净空奖励 (clearance)** ⚠️⚠️ 极高累积风险
- **权重**: 
  - 近距离（<20m）: `CLEARANCE_WEIGHT_NEAR = 8.0`
  - 远距离（>100m）: `CLEARANCE_WEIGHT_FAR = 0.2`
- **计算方式**: `d_change / clearance_d_max`（归一化到[-1, 1]）
- **返回值范围**: -1.0 到 +1.0
- **每步累积**: 
  - 近距离: 8.0 × 1.0 = **8.0**（最大值）
  - 远距离: 0.2 × 1.0 = **0.2**（最大值）
- **2800步累积**: 
  - 近距离: 8.0 × 2800 = **22,400**（最大值，非常危险！）
  - 远距离: 0.2 × 2800 = **560**（相对安全）
- **问题**: 近距离时权重极高，智能体可以通过"保持安全距离"来刷分

#### 6. **全局奖励 (global)** ⚠️⚠️ 极高累积风险
- **权重**: `GLOBAL_WEIGHT = 5.0`
- **计算方式**: 
  - `avg_progress`模式: `np.mean(progresses) * 10.0`
  - `success_rate`模式: `np.mean(successes) * success_reward_value`
- **返回值范围**: 
  - `avg_progress`模式: 约 0.0 到 10.0（取决于平均进展）
  - `success_rate`模式: 约 0.0 到 3000.0（取决于成功率）
- **每步累积**: 
  - `avg_progress`模式: 5.0 × 10.0 = **50.0**（最大值）
  - `success_rate`模式: 5.0 × 3000.0 = **15,000.0**（所有智能体成功时）
- **2800步累积**: 
  - `avg_progress`模式: 50.0 × 2800 = **140,000**（最大值，极其危险！）
  - `success_rate`模式: 15,000.0 × 2800 = **42,000,000**（所有智能体成功时，但实际为0）
- **问题**: 这是最危险的累积项！`avg_progress`模式会导致大量累积

#### 7. **塑形奖励 (shaping)** ⚠️ 中等累积风险
- **权重**: `SHAPING_WEIGHT = 0.3`
- **计算方式**: `gamma * phi_now - phi_last`（潜势函数差分）
- **返回值范围**: 约 -0.1 到 +0.1（取决于距离变化）
- **每步累积**: 0.3 × 0.1 = **0.03**（最大值）
- **2800步累积**: 0.03 × 2800 = **84**（相对较小）
- **问题**: 累积量相对较小，但仍然是每步计算

#### 8. **碰撞惩罚 (collision)** ⚠️ 负累积
- **权重**: `COLLISION_WEIGHT = 4.0`
- **计算方式**: `-collision_penalty_value`（发生碰撞时）
- **返回值范围**: `-80.0`（每次碰撞）
- **每步累积**: 4.0 × (-80.0) = **-320.0**（发生碰撞时）
- **2800步累积**: -320.0 × 碰撞次数（如果700次碰撞 = -224,000）
- **问题**: 虽然惩罚很强，但如果其他奖励项累积足够大，仍可能被抵消

### 2.2 条件性奖励项（不会大量累积）

#### 1. **成功奖励 (success)**
- **权重**: `SUCCESS_WEIGHT = 2.0`
- **计算方式**: 只在到达目标时给一次 `success_reward_value = 3000.0`
- **返回值**: 3000.0 × 2.0 = **6,000**（一次性）
- **问题**: 由于成功率极低（0%），这个奖励几乎从未发放

#### 2. **探索奖励 (exploration)**
- **权重**: `EXPLORATION_WEIGHT = 1.0`
- **计算方式**: 只在访问新格子时给 `5.0`
- **返回值**: 1.0 × 5.0 = **5.0**（每次访问新格子）
- **问题**: 累积量相对较小，因为新格子数量有限

#### 3. **停滞惩罚 (stationary)**
- **权重**: `STATIONARY_WEIGHT = 0.3`
- **计算方式**: 只在停滞>10步时给 `-5.0 到 -20.0`
- **返回值**: 0.3 × (-20.0) = **-6.0**（最大值）
- **问题**: 累积量相对较小

---

## 三、累积量估算

### 3.1 理想情况（所有奖励项都达到最大值）

假设智能体在理想高度、持续接近目标、保持安全距离、有进展：

| 奖励项 | 每步累积 | 2800步累积 |
|--------|---------|------------|
| distance | 2.4 | 6,720 |
| approach | 0.4 | 1,120 |
| direction | 1.5 | 4,200 |
| height | 3.5 | 9,800 |
| clearance (近距离) | 8.0 | 22,400 |
| global (avg_progress) | 50.0 | **140,000** |
| shaping | 0.03 | 84 |
| **总计（正奖励）** | **65.83** | **184,324** |

**注意**: 这还不包括环境中的奖励缩放（`REWARD_POS_SCALE = 2.0`），实际累积可能更高！

### 3.2 实际情况（考虑碰撞惩罚）

假设平均700次碰撞/回合：

| 项目 | 累积量 |
|------|--------|
| 正奖励累积 | 184,324 |
| 碰撞惩罚 | -224,000 (700 × -320) |
| **净奖励** | **-39,676** |

但实际奖励值在400万-540万，说明：
1. **环境奖励缩放** (`REWARD_POS_SCALE = 2.0`) 会将正奖励放大2倍
2. **全局奖励的`avg_progress`模式**可能是主要贡献者
3. 可能存在其他未发现的累积机制

---

## 四、关键问题诊断

### 4.1 最危险的奖励项

#### 🚨 **全局奖励 (global) - avg_progress模式**
- **问题**: 每步计算 `np.mean(progresses) * 10.0`，权重5.0
- **累积量**: 50.0 × 2800 = **140,000**（仅此一项！）
- **影响**: 即使智能体不完成任务，只要持续有进展就能获得大量奖励
- **建议**: 
  - 改为`success_rate`模式（只在所有智能体成功时给奖励）
  - 或大幅降低权重（5.0 → 0.5）

#### 🚨 **净空奖励 (clearance) - 近距离模式**
- **问题**: 近距离时权重8.0，每步可能累积8.0
- **累积量**: 8.0 × 2800 = **22,400**（仅此一项！）
- **影响**: 智能体可以通过"保持安全距离但不接近目标"来刷分
- **建议**: 
  - 降低近距离权重（8.0 → 2.0）
  - 或改为仅在距离减少时给奖励，距离增加时不给奖励

#### 🚨 **高度奖励 (height)**
- **问题**: 权重3.5，每步可能累积3.5
- **累积量**: 3.5 × 2800 = **9,800**（仅此一项！）
- **影响**: 智能体可以通过"保持在理想高度但不接近目标"来刷分
- **建议**: 
  - 降低权重（3.5 → 1.5）
  - 或改为仅在高度不在理想范围时给惩罚，在理想范围时不给奖励

#### ⚠️ **接近目标奖励 (approach)**
- **问题**: 每步接近目标时给奖励，但到达目标时不给额外奖励
- **累积量**: 0.4 × 2800 = **1,120**（持续接近时）
- **影响**: 智能体可以通过"持续接近但不到达"来刷分
- **建议**: 
  - 降低权重（0.8 → 0.2）
  - 或改为仅在距离目标<10m时给奖励，远距离时不给奖励

### 4.2 奖励缩放问题

**环境奖励缩放** (`REWARD_POS_SCALE = 2.0`):
- 所有正奖励会被放大2倍
- 184,324 × 2.0 = **368,648**
- 这解释了为什么奖励值在400万-540万范围内

**可能还有其他缩放机制**，需要进一步检查代码。

---

## 五、改进建议

### 5.1 立即修复（高优先级）

1. **修改全局奖励模式**
   ```bash
   export GLOBAL_REWARD_MODE=success_rate  # 从avg_progress改为success_rate
   export GLOBAL_WEIGHT=5.0  # 保持权重，但只在成功时给奖励
   ```

2. **降低净空奖励权重（近距离）**
   ```bash
   export CLEARANCE_WEIGHT_NEAR=2.0  # 从8.0降低到2.0
   ```

3. **降低高度奖励权重**
   ```bash
   export HEIGHT_WEIGHT=1.5  # 从3.5降低到1.5
   ```

4. **降低接近目标奖励权重**
   ```bash
   export APPROACH_WEIGHT=0.2  # 从0.8降低到0.2
   ```

### 5.2 中期优化（中优先级）

1. **调整奖励缩放**
   ```bash
   export REWARD_POS_SCALE=1.0  # 从2.0降低到1.0，避免过度放大
   ```

2. **修改接近目标奖励逻辑**
   - 仅在距离目标<10m时给奖励
   - 远距离时不给奖励，避免"刷分"

3. **修改净空奖励逻辑**
   - 仅在距离减少时给惩罚
   - 距离增加时不给奖励，避免"刷分"

### 5.3 长期优化（低优先级）

1. **重新设计奖励函数**
   - 将"每步累积奖励"改为"里程碑奖励"
   - 只在达到特定目标时给奖励（如距离减少10%、到达中间点等）

2. **引入奖励衰减**
   - 对于每步计算的奖励项，引入衰减机制
   - 例如：距离奖励在接近目标时逐渐减少

---

## 六、总结

### 6.1 主要问题

1. **全局奖励 (global) - avg_progress模式**是最危险的累积项，单一项就能累积140,000
2. **净空奖励 (clearance) - 近距离模式**累积22,400，权重过高
3. **高度奖励 (height)**累积9,800，权重过高
4. **环境奖励缩放** (`REWARD_POS_SCALE = 2.0`) 将所有正奖励放大2倍

### 6.2 累积机制

- **每步计算的奖励项**会累积2800次（每回合步数）
- **权重 × 返回值 × 步数** = 总累积量
- **环境奖励缩放**会进一步放大累积量

### 6.3 解决方案

优先修复以下4项：
1. 修改全局奖励模式为`success_rate`
2. 降低净空奖励权重（近距离）到2.0
3. 降低高度奖励权重到1.5
4. 降低接近目标奖励权重到0.2

这些修改应该能显著减少"刷分"行为，让奖励信号更准确地反映任务完成情况。
