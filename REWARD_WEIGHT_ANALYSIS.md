# 奖励权重配置分析：局部最优问题诊断

## 一、当前权重配置总结

| 奖励分项 | 权重 | 典型值范围 | 加权后贡献 | 问题 |
|---------|------|-----------|-----------|------|
| **距离奖励** | 1.2 | 0-10 | 0-12 | ⚠️ 偏低 |
| **接近奖励** | 0.1 | -5~+10 | -0.5~+1.0 | ❌ 过低 |
| **探索奖励** | 0.6 | 0-5 (新格子) | 0-3 | ⚠️ 偏低 |
| **停滞惩罚** | 0.3 | -5~0 | -1.5~0 | ✅ 合理 |
| **成功奖励** | 1.5 | 0或15000 | 0或22500 | ⚠️ 一次性，权重偏低 |
| **碰撞惩罚** | 5.8 | -65 (单次) | -377 | ❌ **过高** |
| **净空奖励** | 3.5 (基础) | -10~+20 | -35~+70 | ⚠️ 较高 |
| **高度奖励** | 0.75 | -5~+5 | -3.75~+3.75 | ✅ 合理 |
| **方向奖励** | 0.4 | 0-10 | 0-4 | ✅ 合理 |
| **能量奖励** | 0.2 | -2~+2 | -0.4~+0.4 | ✅ 合理 |

## 二、核心问题分析

### 2.1 碰撞惩罚过高导致过度保守

**问题**：
- `COLLISION_WEIGHT = 5.8`，单次碰撞惩罚值 `COLLISION_PENALTY_VALUE = 65.0`
- 加权后：`-65.0 × 5.8 = -377.0`
- 这个惩罚值**远大于**其他所有奖励的总和

**影响**：
1. 智能体过于害怕碰撞，不敢探索接近目标
2. 在接近目标时（需要冒险穿越障碍）会选择"安全绕行"而非"直接接近"
3. 导致陷入"安全但无法到达目标"的局部最优

**证据**：
- 注释说"降低碰撞惩罚（0.8→0.4）"，但实际值是5.8，与注释矛盾
- 成功奖励（22500）虽然高，但只在到达目标时一次性给予，无法抵消过程中的碰撞恐惧

### 2.2 探索奖励与碰撞惩罚失衡

**问题**：
- 探索奖励权重 `0.6`，新格子奖励约 `5.0`，加权后约 `3.0`
- 碰撞惩罚加权后 `-377.0`
- **比例失衡**：探索奖励无法激励智能体冒险探索

**影响**：
1. 智能体选择"安全区域绕圈"而非"探索新路径"
2. 探索奖励鼓励访问新格子，但碰撞惩罚阻止接近障碍，形成矛盾

### 2.3 接近奖励过低

**问题**：
- `APPROACH_WEIGHT = 0.1`，接近奖励值 `dist_change × 2.0`
- 加权后：每接近1米约 `0.2` 奖励
- 相对于碰撞惩罚 `-377.0`，接近奖励几乎可以忽略

**影响**：
1. 智能体在接近目标时缺乏动力
2. 接近目标的每一步奖励太小，无法抵消碰撞恐惧

### 2.4 净空奖励权重过高

**问题**：
- `CLEARANCE_WEIGHT = 3.5`（基础值），虽然有条件化调整，但基础权重仍然很高
- 近距离时 `CLEARANCE_WEIGHT_NEAR = 5.0`，惩罚权重 `CLEARANCE_PENALTY_WEIGHT = 4.5`
- 可能导致智能体过度保持距离，不敢接近目标

### 2.5 成功奖励权重不足

**问题**：
- `SUCCESS_WEIGHT = 1.5`，成功奖励值 `15000.0`
- 加权后：`22500.0`（一次性）
- 虽然绝对值大，但只在到达目标时给予，无法指导过程中的行为

**影响**：
1. 成功奖励无法抵消过程中的碰撞恐惧
2. 智能体无法建立"冒险接近目标→获得成功奖励"的关联

## 三、局部最优的具体表现

### 3.1 "安全绕圈"策略

**现象**：
- 智能体在安全区域（远离障碍）反复绕圈
- 探索奖励鼓励访问新格子，但碰撞惩罚阻止接近障碍
- 结果：智能体学会"在安全区域探索"而非"向目标前进"

### 3.2 "不敢接近"策略

**现象**：
- 智能体接近目标时，由于碰撞惩罚过高，选择"保持距离"而非"直接接近"
- 净空奖励进一步强化"保持距离"行为
- 结果：智能体在目标附近徘徊，但不敢真正接近

### 3.3 "过早放弃"策略

**现象**：
- 智能体在遇到障碍时，由于碰撞惩罚过高，选择"后退"而非"尝试穿越"
- 接近奖励过低，无法激励"冒险接近"
- 结果：智能体在遇到第一个障碍时就放弃，无法学习复杂路径

## 四、修复建议

### 4.1 降低碰撞惩罚权重

```bash
# 当前值
export COLLISION_WEIGHT=${COLLISION_WEIGHT:-5.8}

# 建议值
export COLLISION_WEIGHT=${COLLISION_WEIGHT:-2.0}  # 降低到2.0
```

**理由**：
- 碰撞惩罚应该足够大以阻止碰撞，但不应阻止探索
- 2.0的权重仍然能提供 `-65.0 × 2.0 = -130.0` 的惩罚，足够阻止碰撞
- 但不会完全阻止接近目标的探索行为

### 4.2 提高接近奖励权重

```bash
# 当前值
export APPROACH_WEIGHT=${APPROACH_WEIGHT:-0.1}

# 建议值
export APPROACH_WEIGHT=${APPROACH_WEIGHT:-0.5}  # 提高到0.5
```

**理由**：
- 接近奖励是到达目标的核心驱动力
- 0.5的权重能让每接近1米获得约 `1.0` 奖励，提供足够的接近动力

### 4.3 提高探索奖励权重

```bash
# 当前值
export EXPLORATION_WEIGHT=${EXPLORATION_WEIGHT:-0.6}

# 建议值
export EXPLORATION_WEIGHT=${EXPLORATION_WEIGHT:-1.0}  # 提高到1.0
```

**理由**：
- 探索奖励应该能够激励智能体探索新路径
- 1.0的权重能让新格子奖励达到约 `5.0`，提供足够的探索动力

### 4.4 降低净空奖励基础权重

```bash
# 当前值
export CLEARANCE_WEIGHT=${CLEARANCE_WEIGHT:-3.5}

# 建议值
export CLEARANCE_WEIGHT=${CLEARANCE_WEIGHT:-1.5}  # 降低到1.5
```

**理由**：
- 净空奖励应该鼓励保持安全距离，但不应该阻止接近目标
- 1.5的权重仍然能提供足够的避障引导，但不会过度限制接近行为

### 4.5 提高成功奖励权重

```bash
# 当前值
export SUCCESS_WEIGHT=${SUCCESS_WEIGHT:-1.5}

# 建议值
export SUCCESS_WEIGHT=${SUCCESS_WEIGHT:-2.5}  # 提高到2.5
```

**理由**：
- 成功奖励是最终目标，应该足够突出
- 2.5的权重能让成功奖励达到 `37500.0`，提供更强的目标导向

### 4.6 调整碰撞惩罚值（可选）

```bash
# 当前值
export COLLISION_PENALTY_VALUE=${COLLISION_PENALTY_VALUE:-65.0}

# 建议值（如果降低权重后仍然过高）
export COLLISION_PENALTY_VALUE=${COLLISION_PENALTY_VALUE:-50.0}  # 降低到50.0
```

**理由**：
- 如果降低权重后仍然过高，可以考虑降低惩罚值本身
- 50.0的惩罚值仍然足够阻止碰撞，但不会完全阻止探索

## 五、权重平衡原则

### 5.1 奖励/惩罚比例

**原则**：
- 单步惩罚不应超过单步奖励的10倍
- 目标奖励应该足够大，能够抵消过程中的惩罚

**当前问题**：
- 碰撞惩罚 `-377.0` 远大于接近奖励 `+0.2`（约1885倍）
- 成功奖励虽然大，但只在到达时给予，无法指导过程

### 5.2 探索/安全平衡

**原则**：
- 探索奖励应该能够激励探索新路径
- 碰撞惩罚应该阻止碰撞，但不应该阻止探索

**当前问题**：
- 探索奖励 `+3.0` 无法抵消碰撞恐惧 `-377.0`
- 智能体选择"安全绕圈"而非"探索接近"

### 5.3 过程/目标平衡

**原则**：
- 过程奖励（接近、探索）应该能够指导行为
- 目标奖励（成功）应该足够突出，但不应完全依赖

**当前问题**：
- 接近奖励过低，无法提供足够的接近动力
- 成功奖励只在到达时给予，无法指导过程中的行为

## 六、推荐的权重配置

```bash
# === 核心奖励权重（修复局部最优）===
export DISTANCE_WEIGHT=${DISTANCE_WEIGHT:-1.5}          # 提高到1.5
export APPROACH_WEIGHT=${APPROACH_WEIGHT:-0.5}          # 提高到0.5（从0.1）
export EXPLORATION_WEIGHT=${EXPLORATION_WEIGHT:-1.0}    # 提高到1.0（从0.6）
export SUCCESS_WEIGHT=${SUCCESS_WEIGHT:-2.5}            # 提高到2.5（从1.5）

# === 安全约束权重（降低过度保守）===
export COLLISION_WEIGHT=${COLLISION_WEIGHT:-2.0}        # 降低到2.0（从5.8）
export CLEARANCE_WEIGHT=${CLEARANCE_WEIGHT:-1.5}        # 降低到1.5（从3.5）

# === 其他权重（保持合理）===
export STATIONARY_WEIGHT=${STATIONARY_WEIGHT:-0.3}       # 保持0.3
export HEIGHT_WEIGHT=${HEIGHT_WEIGHT:-0.75}             # 保持0.75
export DIRECTION_WEIGHT=${DIRECTION_WEIGHT:-0.4}         # 保持0.4
export ENERGY_WEIGHT=${ENERGY_WEIGHT:-0.2}              # 保持0.2
```

## 七、验证方法

### 7.1 训练指标

观察以下指标是否改善：
1. **成功率**：是否提高
2. **平均距离**：是否降低（更接近目标）
3. **碰撞次数**：是否在合理范围内（不应完全避免，也不应过多）
4. **探索覆盖率**：是否提高（访问更多新区域）

### 7.2 行为观察

观察智能体行为是否改善：
1. **是否敢于接近目标**：不应在目标附近徘徊
2. **是否探索新路径**：不应在安全区域反复绕圈
3. **是否冒险穿越障碍**：在必要时应该尝试接近，而非完全回避

## 八、总结

**核心问题**：
1. 碰撞惩罚过高（5.8），导致过度保守
2. 接近奖励过低（0.1），无法提供接近动力
3. 探索奖励偏低（0.6），无法激励探索新路径
4. 净空奖励过高（3.5），进一步强化"保持距离"行为

**修复方向**：
1. 降低碰撞惩罚权重（5.8 → 2.0）
2. 提高接近奖励权重（0.1 → 0.5）
3. 提高探索奖励权重（0.6 → 1.0）
4. 降低净空奖励权重（3.5 → 1.5）
5. 提高成功奖励权重（1.5 → 2.5）

**预期效果**：
- 智能体更敢于接近目标
- 更愿意探索新路径
- 在遇到障碍时更愿意尝试接近，而非完全回避
- 减少"安全绕圈"和"不敢接近"的局部最优行为


