# PER样本选择分析：哪些经验样本会被高概率选取？

## 一、当前优先级计算公式

**代码位置**：`paper3d_train_optimized.py:2038-2049`

```python
# 优先级信号组合
signal = priority_td_weight * TD_error + priority_reward_weight * reward_abs

# 优先级计算
priority = (signal^negative_slope) * age_weight

其中：
- priority_td_weight = 1.1      # TD误差权重
- priority_reward_weight = 0.12   # 奖励幅值权重
- negative_slope = 0.6            # 优先级幂次（默认）
- age_weight = priority_age_decay^age  # 年龄衰减（0.96^age）
```

**当前配置**（从`run_optimized.sh`）：
- `PER_TD_WEIGHT=1.1`
- `PER_REWARD_WEIGHT=0.12`
- `PER_AGE_DECAY=0.96`（有衰减，旧样本优先级会降低）

---

## 二、TD误差分析

### 2.1 TD误差计算公式

**代码位置**：`paper3d_train_optimized.py:7330-7333`（MADDPG）或 `10258-10259`（MATD3）

```python
# TD误差 = 实际回报 - 预测Q值
TD = r + gamma * (1-d) * Q'(s', a') - Q(s, a)

其中：
- r: 即时奖励
- gamma: 折扣因子（通常0.95-0.99）
- Q'(s', a'): 目标网络预测的下一状态Q值
- Q(s, a): 当前网络预测的当前状态Q值
```

### 2.2 哪些样本会有高TD误差？

**高TD误差 = 网络预测不准确 = 需要更多学习的样本**

#### 情况1：网络低估了奖励（TD误差为正且大）

**特征**：
- 实际奖励很高，但网络预测的Q值很低
- `TD = 高奖励 - 低Q值 = 大正值`

**典型场景**：
1. **成功到达目标**：
   - 实际奖励：成功奖励3000.0 + 悬停奖励 + 其他正奖励
   - 网络初期可能低估：Q值可能只有几百
   - **TD误差**：可能达到2000-3000

2. **大幅接近目标**：
   - 实际奖励：距离奖励 + 方向奖励 + 探索奖励
   - 网络可能低估：Q值预测不准确
   - **TD误差**：可能达到几百

3. **高奖励但网络未学习到**：
   - 实际奖励：大量正奖励累积（距离、探索、方向等）
   - 网络预测：Q值较低
   - **TD误差**：可能达到几百到上千

#### 情况2：网络高估了奖励（TD误差为负且绝对值大）

**特征**：
- 实际奖励很低（或负），但网络预测的Q值很高
- `TD = 低奖励 - 高Q值 = 大负值`
- **取绝对值后**：`|TD| = 大正值`

**典型场景**：
1. **碰撞/穿透地形**：
   - 实际奖励：大量碰撞惩罚（-174.0 × 碰撞次数）
   - 网络可能高估：Q值预测为正或较小负值
   - **TD误差**：可能达到几百到上千（绝对值）

2. **策略突然变差**：
   - 实际奖励：突然大幅下降
   - 网络预测：仍保持较高Q值
   - **TD误差**：可能达到几百（绝对值）

3. **环境变化但网络未适应**：
   - 实际奖励：地形复杂度提升导致奖励下降
   - 网络预测：仍基于旧环境的Q值
   - **TD误差**：可能达到几百（绝对值）

---

## 三、奖励幅值分析

### 3.1 奖励幅值计算

**代码位置**：`paper3d_train_optimized.py:2016-2021`

```python
# 奖励幅值 = 所有智能体奖励绝对值的平均
rew_abs = float(np.mean(np.abs(self.rew[int(idx)])))
rew_abs = min(rew_abs, 1000.0)  # 限制在1000以内
```

### 3.2 哪些样本会有高奖励幅值？

**高奖励幅值 = 奖励绝对值大 = 可能是高正奖励或高负奖励**

#### 情况1：高正奖励（奖励幅值大）

**典型场景**：
1. **成功到达目标**：
   - 成功奖励：3000.0（一次性）
   - 悬停奖励：持续累积（每步可能几十到几百）
   - 其他奖励：距离、方向、探索等
   - **奖励幅值**：可能达到3000-5000

2. **接近目标且无碰撞**：
   - 距离奖励：每步可能几十
   - 方向奖励：每步可能几十
   - 探索奖励：每步可能几十
   - 净空奖励：每步可能几十
   - **奖励幅值**：可能达到几百

3. **探索新区域**：
   - 探索奖励：访问新格子 +5.0（权重0.6后=3.0）
   - 其他奖励：距离、方向等
   - **奖励幅值**：可能达到几十到几百

#### 情况2：高负奖励（奖励幅值大）

**典型场景**：
1. **大量碰撞**：
   - 碰撞惩罚：-174.0 × 碰撞次数
   - 如果每步都碰撞：-174.0
   - **奖励幅值**：174.0

2. **穿透地形**：
   - 地形穿透惩罚：可能-280.0或更高
   - **奖励幅值**：280.0或更高

3. **远离目标且碰撞**：
   - 距离惩罚：负奖励
   - 碰撞惩罚：-174.0
   - **奖励幅值**：可能达到几百

---

## 四、优先级计算示例

### 4.1 示例1：成功到达目标（高优先级）

**假设**：
- TD误差：2500（网络低估了成功奖励）
- 奖励幅值：3500（成功奖励3000 + 其他奖励）
- 年龄：0（新样本）

**计算**：
```
signal = 1.1 * 2500 + 0.12 * 3500
       = 2750 + 420
       = 3170

priority = (3170^0.6) * (0.96^0)
         = 3170^0.6 * 1.0
         ≈ 3170^0.6
         ≈ 3170^0.6 ≈ 3170^0.6 ≈ 3170^0.6
```

**结果**：**极高优先级**（会被频繁选中）

### 4.2 示例2：大量碰撞（高优先级）

**假设**：
- TD误差：800（网络高估了奖励，实际是大量碰撞）
- 奖励幅值：500（大量碰撞惩罚）
- 年龄：0（新样本）

**计算**：
```
signal = 1.1 * 800 + 0.12 * 500
       = 880 + 60
       = 940

priority = (940^0.6) * 1.0
         ≈ 940^0.6
```

**结果**：**高优先级**（会被频繁选中，学习避免碰撞）

### 4.3 示例3：普通样本（中等优先级）

**假设**：
- TD误差：50（网络预测较准确）
- 奖励幅值：20（普通奖励）
- 年龄：0（新样本）

**计算**：
```
signal = 1.1 * 50 + 0.12 * 20
       = 55 + 2.4
       = 57.4

priority = (57.4^0.6) * 1.0
         ≈ 57.4^0.6
```

**结果**：**中等优先级**（会被选中，但频率较低）

### 4.4 示例4：旧样本（低优先级）

**假设**：
- TD误差：2500（高）
- 奖励幅值：3500（高）
- 年龄：100（旧样本）

**计算**：
```
signal = 1.1 * 2500 + 0.12 * 3500
       = 3170

age_weight = 0.96^100 ≈ 0.017

priority = (3170^0.6) * 0.017
         ≈ 3170^0.6 * 0.017
```

**结果**：**优先级降低**（年龄衰减导致优先级大幅降低）

---

## 五、总结：哪些样本会被高概率选取？

### 5.1 高优先级样本特征（按优先级排序）

#### 🥇 **最高优先级：成功到达目标 + 网络低估**

**特征**：
- ✅ TD误差：**2000-3000**（网络严重低估成功奖励）
- ✅ 奖励幅值：**3000-5000**（成功奖励 + 其他奖励）
- ✅ 年龄：**新样本**（age=0，无衰减）

**优先级计算**：
```
signal = 1.1 * 2500 + 0.12 * 4000 = 2750 + 480 = 3230
priority = 3230^0.6 ≈ 极高
```

**为什么会被选中**：
- 网络需要学习"成功到达目标"的价值
- 这些样本提供了最重要的学习信号

#### 🥈 **第二优先级：大量碰撞 + 网络高估**

**特征**：
- ✅ TD误差：**500-1500**（网络高估了奖励，实际是大量碰撞）
- ✅ 奖励幅值：**200-500**（大量碰撞惩罚）
- ✅ 年龄：**新样本**

**优先级计算**：
```
signal = 1.1 * 1000 + 0.12 * 300 = 1100 + 36 = 1136
priority = 1136^0.6 ≈ 高
```

**为什么会被选中**：
- 网络需要学习"避免碰撞"的重要性
- 这些样本纠正网络的高估

#### 🥉 **第三优先级：大幅接近目标 + 网络低估**

**特征**：
- ✅ TD误差：**300-800**（网络低估了接近目标的奖励）
- ✅ 奖励幅值：**100-300**（距离、方向、探索等奖励）
- ✅ 年龄：**新样本**

**优先级计算**：
```
signal = 1.1 * 500 + 0.12 * 200 = 550 + 24 = 574
priority = 574^0.6 ≈ 中等偏高
```

**为什么会被选中**：
- 网络需要学习"接近目标"的策略
- 这些样本提供了有效的学习信号

#### 4️⃣ **第四优先级：环境变化 + 网络未适应**

**特征**：
- ✅ TD误差：**200-500**（环境变化导致奖励变化，但网络未适应）
- ✅ 奖励幅值：**50-200**（奖励变化）
- ✅ 年龄：**新样本**

**优先级计算**：
```
signal = 1.1 * 300 + 0.12 * 100 = 330 + 12 = 342
priority = 342^0.6 ≈ 中等
```

**为什么会被选中**：
- 网络需要适应环境变化
- 这些样本帮助网络快速适应

### 5.2 低优先级样本特征

#### ❌ **低优先级：普通样本 + 网络预测准确**

**特征**：
- ❌ TD误差：**< 100**（网络预测较准确）
- ❌ 奖励幅值：**< 50**（普通奖励）
- ❌ 年龄：**任意**

**优先级计算**：
```
signal = 1.1 * 50 + 0.12 * 20 = 55 + 2.4 = 57.4
priority = 57.4^0.6 ≈ 低
```

**为什么优先级低**：
- 网络已经学会了这些样本
- 重复学习这些样本效率低

#### ❌ **最低优先级：旧样本（即使TD误差高）**

**特征**：
- ✅ TD误差：**高**（但样本很旧）
- ✅ 奖励幅值：**高**（但样本很旧）
- ❌ 年龄：**> 50**（旧样本，有衰减）

**优先级计算**：
```
signal = 1.1 * 2500 + 0.12 * 4000 = 3230
age_weight = 0.96^100 ≈ 0.017
priority = 3230^0.6 * 0.017 ≈ 低（被年龄衰减大幅降低）
```

**为什么优先级低**：
- 旧样本可能不再代表当前策略
- 年龄衰减机制降低旧样本优先级

---

## 六、实际训练中的样本选择模式

### 6.1 训练初期（网络预测不准确）

**高优先级样本**：
1. **所有成功样本**（TD误差极高）
2. **所有碰撞样本**（TD误差高，网络高估）
3. **所有接近目标样本**（TD误差中等）

**选择模式**：
- 70% PER采样：优先选择上述高优先级样本
- 30% 均匀采样：保持探索

### 6.2 训练中期（网络部分学习）

**高优先级样本**：
1. **部分成功样本**（TD误差仍然较高）
2. **复杂场景碰撞**（网络仍高估某些场景）
3. **边界情况**（网络预测不准确的特殊情况）

**选择模式**：
- 网络已经学会了简单场景
- 重点关注复杂场景和边界情况

### 6.3 训练后期（网络预测较准确）

**高优先级样本**：
1. **罕见成功样本**（偶尔成功，TD误差仍然较高）
2. **极端碰撞场景**（特殊地形或障碍物配置）
3. **策略变化样本**（网络策略调整时的样本）

**选择模式**：
- 大部分样本TD误差较低
- 只有少数特殊样本被优先选择

---

## 七、关键发现

### 7.1 当前配置下的样本选择倾向

**基于当前配置**（`PER_TD_WEIGHT=1.1`, `PER_REWARD_WEIGHT=0.12`）：

1. **TD误差主导**（权重1.1 vs 0.12）：
   - TD误差的贡献是奖励幅值的**9.2倍**（1.1/0.12）
   - **结论**：样本选择主要基于TD误差，奖励幅值影响较小

2. **成功样本最优先**：
   - 成功样本的TD误差通常最高（2000-3000）
   - 即使奖励幅值权重较低，成功样本仍会被优先选择

3. **碰撞样本次优先**：
   - 碰撞样本的TD误差较高（500-1500）
   - 网络需要学习避免碰撞

4. **年龄衰减影响**：
   - 旧样本优先级会随时间降低（0.96^age）
   - 100步后优先级降低到约1.7%

### 7.2 问题：如何选择"安全且姿态好"的样本？

**当前问题**：
- 当前优先级计算**不直接考虑安全距离和姿态**
- 只能通过TD误差和奖励幅值间接影响

**解决方案**：
1. **启用安全距离权重**（`priority_clearance_weight > 0`）
2. **启用姿态权重**（`priority_attitude_weight > 0`）
3. **修改优先级计算公式**，增加安全距离和姿态信号

---

## 八、建议：如何优先选择"安全且姿态好"的样本

### 8.1 方案1：启用安全距离权重

**修改优先级计算公式**：
```python
# 从观察值中提取安全距离
# obs[40]: 法向净空（归一化: /20.0）
# obs[55]: 最近障碍物距离（归一化: /map_size）

clearance_signal = min(obs[40] * 20.0, obs[55] * map_size)  # 反归一化
clearance_bonus = max(0, clearance_signal - 10.0)  # 安全距离>10m时给予奖励

signal = priority_td_weight * TD_error + \
         priority_reward_weight * reward_abs + \
         priority_clearance_weight * clearance_bonus
```

**效果**：
- 安全距离>10m的样本优先级提升
- 安全距离<10m的样本优先级降低

### 8.2 方案2：启用姿态权重

**修改优先级计算公式**：
```python
# 从观察值中提取姿态信息
# obs[3:6]: 速度（归一化: /22.5）
# obs[56:58]: 目标方向（归一化向量）

velocity = obs[3:6] * 22.5  # 反归一化速度
goal_dir = obs[56:58]  # 目标方向

# 计算速度与目标方向的对齐度（点积）
alignment = np.dot(velocity / (np.linalg.norm(velocity) + 1e-6), goal_dir)
attitude_bonus = max(0, alignment)  # 对齐度越高，奖励越大

signal = priority_td_weight * TD_error + \
         priority_reward_weight * reward_abs + \
         priority_attitude_weight * attitude_bonus
```

**效果**：
- 速度与目标方向对齐的样本优先级提升
- 速度与目标方向不对齐的样本优先级降低

### 8.3 方案3：组合使用

**修改优先级计算公式**：
```python
signal = priority_td_weight * TD_error + \
         priority_reward_weight * reward_abs + \
         priority_clearance_weight * clearance_bonus + \
         priority_attitude_weight * attitude_bonus
```

**推荐权重**：
- `priority_clearance_weight = 0.5-1.0`（安全距离权重）
- `priority_attitude_weight = 0.3-0.5`（姿态权重）

**效果**：
- 优先选择"安全距离>10m且速度与目标方向对齐"的样本
- 这些样本通常代表"好的策略"

---

## 九、总结

### 9.1 当前会被高概率选取的样本

**按优先级排序**：

1. **🥇 成功到达目标 + 网络低估**（TD误差2000-3000）
2. **🥈 大量碰撞 + 网络高估**（TD误差500-1500）
3. **🥉 大幅接近目标 + 网络低估**（TD误差300-800）
4. **4️⃣ 环境变化 + 网络未适应**（TD误差200-500）
5. **❌ 普通样本**（TD误差<100，优先级低）
6. **❌ 旧样本**（即使TD误差高，但年龄衰减导致优先级低）

### 9.2 关键发现

1. **TD误差主导**：当前配置下，TD误差权重（1.1）远大于奖励幅值权重（0.12）
2. **成功样本最优先**：成功到达目标的样本几乎总是被优先选择
3. **碰撞样本次优先**：大量碰撞的样本也会被优先选择（学习避免碰撞）
4. **年龄衰减有效**：旧样本优先级会随时间降低

### 9.3 如何选择"安全且姿态好"的样本

**当前问题**：优先级计算不直接考虑安全距离和姿态

**解决方案**：
1. 启用 `priority_clearance_weight`（安全距离权重）
2. 启用 `priority_attitude_weight`（姿态权重）
3. 修改优先级计算公式，增加这两个信号

**推荐配置**：
```bash
PER_CLEARANCE_WEIGHT=0.8    # 安全距离权重
PER_ATTITUDE_WEIGHT=0.4     # 姿态权重
```

这样，系统会优先选择"离地形较远且保持较好姿态和朝向"的样本。
