# 势场修正与网络动作平衡方案

## 一、问题分析

### 1.1 当前问题

**问题1：势场作用过强**
- `ACTION_FORCE_RATIO = 0.50`（50%势场，50%网络动作）
- `TERRAIN_REPULSION = 3800.0`（地形排斥力非常高）
- `GOAL_ATTRACTION = 15.0`（目标吸引力较高）
- **结果**：势场力过强，网络动作被过度修正，难以学习有效策略

**问题2：FR过低导致修正不足**
- 如果`FR < 0.3`，势场修正作用不足
- 可能导致碰撞或探索不足
- 网络动作无法获得足够的引导

### 1.2 核心矛盾

- **高FR（0.5-0.7）**：势场主导，网络动作被过度修正，难以学习
- **低FR（0.2-0.3）**：网络主导，势场修正不足，可能导致碰撞

## 二、解决方案

### 2.1 方案A：渐进式FR衰减（推荐）

**原理**：
- 训练初期：高FR（0.6-0.7），势场主导，提供强引导
- 训练中期：中FR（0.4-0.5），势场与网络平衡
- 训练后期：低FR（0.2-0.3），网络主导，势场仅提供安全修正

**实现**：
```bash
# 使用现有的 ACTION_FORCE_RATIO_SCHEDULE_PCT
export ACTION_FORCE_RATIO_SCHEDULE_PCT="0%:0.65,30%:0.45,60%:0.30,100%:0.20"
```

**优势**：
- ✅ 训练初期有强引导，避免碰撞
- ✅ 训练后期网络主导，学习有效策略
- ✅ 平滑过渡，避免突变

### 2.2 方案B：降低势场强度

**原理**：
- 降低地形排斥力：`TERRAIN_REPULSION: 3800.0 → 2000.0`
- 降低目标吸引力：`GOAL_ATTRACTION: 15.0 → 10.0`
- 保持FR在中等水平（0.4-0.5）

**实现**：
```bash
export TERRAIN_REPULSION=${TERRAIN_REPULSION:-2000.0}  # 降低到2000
export GOAL_ATTRACTION=${GOAL_ATTRACTION:-10.0}        # 降低到10.0
export ACTION_FORCE_RATIO=${ACTION_FORCE_RATIO:-0.40}  # 降低到0.40
```

**优势**：
- ✅ 势场力更温和，不会过度修正
- ✅ 网络动作有更多表达空间
- ✅ 实现简单

### 2.3 方案C：条件化FR（高级）

**原理**：
- 根据状态动态调整FR：
  - 距离目标远：高FR（0.6），强引导
  - 距离目标近：低FR（0.3），网络主导
  - 碰撞风险高：高FR（0.7），安全修正
  - 碰撞风险低：低FR（0.2），网络探索

**实现**：
需要修改训练代码，根据观察数据动态计算FR。

**优势**：
- ✅ 最灵活，适应不同状态
- ✅ 既能提供引导，又能让网络学习

**劣势**：
- ⚠️ 实现复杂
- ⚠️ 需要额外的超参数调优

### 2.4 方案D：混合方案（推荐）

**原理**：
- 结合方案A和B：
  1. 降低势场强度（方案B）
  2. 使用渐进式FR衰减（方案A）
  3. 保持FR在合理范围（0.2-0.6）

**实现**：
```bash
# 降低势场强度
export TERRAIN_REPULSION=${TERRAIN_REPULSION:-2000.0}  # 从3800降低到2000
export GOAL_ATTRACTION=${GOAL_ATTRACTION:-10.0}        # 从15.0降低到10.0

# 渐进式FR衰减
export ACTION_FORCE_RATIO=${ACTION_FORCE_RATIO:-0.50}  # 初始值（会被schedule覆盖）
export ACTION_FORCE_RATIO_SCHEDULE_PCT="0%:0.60,25%:0.45,50%:0.35,75%:0.25,100%:0.20"
```

**优势**：
- ✅ 势场强度适中，不会过度修正
- ✅ FR渐进衰减，训练后期网络主导
- ✅ 实现简单，易于调优

## 三、推荐配置

### 3.1 保守配置（稳定优先）

```bash
# 势场参数（适度降低）
export TERRAIN_REPULSION=${TERRAIN_REPULSION:-2500.0}  # 从3800降低到2500
export GOAL_ATTRACTION=${GOAL_ATTRACTION:-12.0}        # 从15.0降低到12.0
export MAX_FORCE_MAGNITUDE=${MAX_FORCE_MAGNITUDE:-18}  # 从20降低到18

# FR配置（渐进衰减）
export ACTION_FORCE_RATIO=${ACTION_FORCE_RATIO:-0.50}
export ACTION_FORCE_RATIO_SCHEDULE_PCT="0%:0.55,30%:0.40,60%:0.30,100%:0.25"
```

### 3.2 激进配置（学习优先）

```bash
# 势场参数（大幅降低）
export TERRAIN_REPULSION=${TERRAIN_REPULSION:-1800.0}  # 从3800降低到1800
export GOAL_ATTRACTION=${GOAL_ATTRACTION:-8.0}         # 从15.0降低到8.0
export MAX_FORCE_MAGNITUDE=${MAX_FORCE_MAGNITUDE:-15}   # 从20降低到15

# FR配置（快速衰减）
export ACTION_FORCE_RATIO=${ACTION_FORCE_RATIO:-0.50}
export ACTION_FORCE_RATIO_SCHEDULE_PCT="0%:0.50,20%:0.35,40%:0.25,60%:0.20,100%:0.15"
```

### 3.3 平衡配置（推荐）

```bash
# 势场参数（适度降低）
export TERRAIN_REPULSION=${TERRAIN_REPULSION:-2200.0}  # 从3800降低到2200（约42%降低）
export GOAL_ATTRACTION=${GOAL_ATTRACTION:-10.0}         # 从15.0降低到10.0（约33%降低）
export MAX_FORCE_MAGNITUDE=${MAX_FORCE_MAGNITUDE:-16}   # 从20降低到16（20%降低）

# FR配置（平滑衰减）
export ACTION_FORCE_RATIO=${ACTION_FORCE_RATIO:-0.50}
export ACTION_FORCE_RATIO_SCHEDULE_PCT="0%:0.60,25%:0.45,50%:0.35,75%:0.25,100%:0.20"
```

## 四、调优建议

### 4.1 观察指标

1. **训练初期（0-25%）**：
   - 观察：是否频繁碰撞？
   - 如果碰撞多：提高初始FR（0.60 → 0.65）
   - 如果碰撞少：可以降低初始FR（0.60 → 0.55）

2. **训练中期（25-75%）**：
   - 观察：网络是否开始学习有效策略？
   - 如果学习慢：降低FR衰减速度（延长高FR时间）
   - 如果学习快：可以加快FR衰减（提前降低FR）

3. **训练后期（75-100%）**：
   - 观察：网络是否能够独立完成任务？
   - 如果仍依赖势场：进一步降低FR（0.20 → 0.15）
   - 如果完全独立：可以保持FR在0.20

### 4.2 势场强度调优

1. **地形排斥力（TERRAIN_REPULSION）**：
   - 如果频繁穿透地形：提高（2000 → 2500）
   - 如果过度修正：降低（2000 → 1800）

2. **目标吸引力（GOAL_ATTRACTION）**：
   - 如果无法接近目标：提高（10.0 → 12.0）
   - 如果过度依赖势场：降低（10.0 → 8.0）

3. **势场力上限（MAX_FORCE_MAGNITUDE）**：
   - 如果势场力过强：降低（16 → 14）
   - 如果势场力不足：提高（16 → 18）

## 五、实施步骤

### 5.1 立即实施（推荐配置）

1. 修改`run_optimized.sh`：
   - 降低势场强度参数
   - 启用渐进式FR衰减

2. 运行训练，观察：
   - 训练初期：碰撞次数、成功率
   - 训练中期：网络学习进度
   - 训练后期：网络独立性

3. 根据观察结果微调参数

### 5.2 进阶优化（可选）

1. 实现条件化FR（方案C）
2. 根据性能指标动态调整FR
3. 实现多阶段训练策略

## 六、预期效果

### 6.1 训练初期（0-25%）

- ✅ 势场提供强引导，避免碰撞
- ✅ 网络开始学习基本策略
- ✅ 成功率逐步提高

### 6.2 训练中期（25-75%）

- ✅ 势场与网络平衡，网络学习加速
- ✅ 网络开始学习复杂策略
- ✅ 成功率快速提高

### 6.3 训练后期（75-100%）

- ✅ 网络主导，势场仅提供安全修正
- ✅ 网络学习到有效策略
- ✅ 高成功率，低碰撞率

## 七、总结

**核心思路**：
1. **降低势场强度**：让势场更温和，不会过度修正
2. **渐进式FR衰减**：训练初期高FR引导，后期低FR让网络主导
3. **保持平衡**：既要有引导，又要让网络学习

**推荐方案**：
- 使用**方案D（混合方案）**
- 采用**平衡配置**
- 根据训练结果微调参数


