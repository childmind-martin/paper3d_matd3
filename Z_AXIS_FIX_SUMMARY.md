# Z轴负向输出修复总结

## 修复日期
2025-11-29

## 问题诊断

Actor网络在训练过程中**始终倾向于让Z轴输出负值**(az ≈ -1.0),导致智能体持续下降、贴近地形飞行。

## 根本原因

### 1. 重力无法抵消 (最关键)
```
重力: 9.81 m/s²
控制增益: 6.8
需要的归一化加速度: 9.81 / 6.8 = 1.44
Actor输出范围: tanh ∈ [-1, 1]

问题: 1.44 > 1.0 → Actor无法输出足够大的加速度来抵消重力!
```

### 2. 初始偏置为零
- Actor输出层Z轴偏置设为0
- 初始输出接近0 → 智能体下降
- 网络学习"避免下降"但学偏了方向

### 3. 奖励函数不对称
- 低高度惩罚: `-height_shortage² × 3.0` (平方增长)
- 高高度惩罚: `-(height_excess) × 0.2` (线性,系数小)
- 穿透惩罚: `-depth² × 50` (灾难性)

### 4. 学习陷入局部最优
- Critic学到"向下相对安全"的错误策略
- Actor强化了"az=-1.0"的极端输出
- 形成恶性循环

## 修复方案

### 修复1: 提高控制增益 ✅

**文件**: `run_optimized.sh` 第169行

**修改前**:
```bash
export CONTROL_ACCEL_GAIN=${CONTROL_ACCEL_GAIN:-6.8}
```

**修改后**:
```bash
export CONTROL_ACCEL_GAIN=${CONTROL_ACCEL_GAIN:-12.0}
```

**效果**:
- 新的悬停需求: `9.81 / 12.0 = 0.82` ← 在tanh范围内!
- Actor输出`az=0.82`即可悬停
- Actor有足够空间向上加速(`az ∈ [0.82, 1.0]`)

### 修复2: 设置正向Z轴偏置 ✅

**文件**: `paper3d_train_optimized.py` 第3591-3605行 & 第6747-6761行

**修改前**:
```python
z_bias_value = 0.0  # Z轴偏置为0
new_bias[2] = z_bias_value
print(f"Z轴偏置={z_bias_value:.4f}（能抵消重力）")
```

**修改后**:
```python
z_bias_value = 0.3  # 正向偏置，鼓励向上输出
new_bias[2] = z_bias_value
print(f"Z轴偏置={z_bias_value:.4f}（正向引导抵消重力）")
```

**效果**:
- 初始输出: `tanh(0.3) ≈ 0.29` (向上)
- 给Actor一个正确的学习起点
- 避免初期就陷入"下降→惩罚→负向学习"的恶性循环

## 预期效果

### 修复前
```
Time Steps: 0 → 2000
az (Agent 0): -1.0 → -1.0  (始终负值)
az (Agent 1): -1.0 → -1.0  (始终负值)
az (Agent 2): -1.0 → -1.0  (始终负值)

轨迹: 贴近地形,频繁触地,无法爬升
```

### 修复后 (预期)
```
Time Steps: 0 → 500
az (Agent 0): 0.3 → 0.5 → 0.7  (初期向上)

Time Steps: 500 → 1500
az (Agent 0): 0.7 → 0.2 → 0.8 → 0.4  (动态调整)

Time Steps: 1500 → 2000
az (Agent 0): 0.4 → 0.6 → 0.3  (稳定在[0,0.8]区间)

轨迹: 保持理想高度,平滑飞行,能爬升越障
```

## 验证要点

### 1. Actor输出分布
- ✅ az的均值应在`[0.2, 0.6]`区间
- ✅ az的标准差应 > 0.2 (有动态调整)
- ✅ az应有正负值变化(不是固定在-1或+1)

### 2. 轨迹表现
- ✅ 智能体离地高度保持在5-15米
- ✅ 能够爬升越过中等高度障碍(10-20米)
- ✅ 触地/穿透次数大幅减少

### 3. 奖励变化
- ✅ 高度奖励从负值变为正值
- ✅ 总奖励提升(从-70000提升到-40000或更高)
- ✅ 地形穿透惩罚次数减少

### 4. Loss行为
- ✅ Actor Loss不再过小(<5)
- ✅ Critic Loss逐步下降并稳定
- ✅ Q值估计更准确(不是全负)

## 测试脚本

```bash
# 运行5回合验证修复
cd /home/tang/Desktop
export NUM_EPISODES=5
/bin/bash run_optimized.sh
```

## 进一步优化建议

### 短期 (立即可做)
1. ✅ **已完成**: 提高控制增益到12.0
2. ✅ **已完成**: 设置Z轴偏置为0.3
3. **建议**: 降低穿透惩罚强度
   ```python
   # vectorized_reward_calculator.py 第864行
   penetration_penalty = -penetration_depth * penetration_depth * 25.0  # 从50降到25
   ```

### 中期 (需要重新训练)
4. **建议**: 平衡高低惩罚
   ```bash
   # run_optimized.sh
   export HEIGHT_IDEAL_MIN=3.0   # 从5.0降到3.0
   export HEIGHT_IDEAL_MAX=20.0  # 从35.0降到20.0
   export HEIGHT_WEIGHT=1.5      # 从3.0降到1.5
   ```

5. **建议**: 改用线性惩罚
   ```python
   # vectorized_reward_calculator.py 第850行
   # 从平方惩罚改为线性惩罚
   rewards[below_range] = -height_shortage * 2.0  # 而不是平方×3.0
   ```

### 长期 (架构优化)
6. **建议**: Curriculum Learning
   - 初期(0-50回合): HEIGHT_WEIGHT=0.5
   - 中期(50-100回合): HEIGHT_WEIGHT=1.0
   - 后期(100+回合): HEIGHT_WEIGHT=1.5

7. **建议**: Z轴输出的正向激励
   ```python
   # 在reward函数中添加
   if hasattr(agent, 'action') and agent.action.u[2] > 0:
       reward += agent.action.u[2] * 1.0  # 奖励向上输出
   ```

## 修复信心等级

⭐⭐⭐⭐⭐ (5/5)

**理由**:
1. ✅ 定位到数学上的不可行性(1.44 > 1.0)
2. ✅ 修复方案直击根本原因(提高增益使0.82 < 1.0)
3. ✅ 配合正向偏置引导学习方向
4. ✅ 修改最小化,不影响其他功能
5. ✅ 物理上合理,数学上正确

## 附加说明

### 为什么是12.0而不是其他值?

```
目标: 让Actor在tanh范围内能够抵消重力

选项1: 增益=10.0 → 需要az=0.98 (太接近1.0,没有向上空间)
选项2: 增益=12.0 → 需要az=0.82 (适中,有向上空间)
选项3: 增益=15.0 → 需要az=0.65 (太小,响应不够灵敏)

选择12.0: 平衡了"能抵消重力"和"保留控制空间"
```

### 为什么偏置是0.3?

```
tanh(0.3) ≈ 0.29

初始加速度: 12.0 × 0.29 = 3.48 m/s² (向上)
净加速度: 3.48 - 9.81 = -6.33 m/s² (仍下降,但比0.0时慢)

目的: 不是让智能体立即向上飞,而是给一个"倾向"
让Critic看到"向上输出能减缓下降速度"
引导Actor学习正确的方向
```

---

**下一步**: 运行训练,观察az输出是否从-1.0变为动态的[0, 0.8]区间。

