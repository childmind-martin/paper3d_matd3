# 网络结构复杂度分析报告

## 当前网络结构

### 1. Actor网络
- **配置**: `ACTOR_HIDDEN="256,256,256"` (3层，每层256)
- **实际使用**: `(256, 256, 256)` (3层)
- **额外特征**:
  - FR特征: 16维嵌入
  - PF特征: 32维嵌入
- **输出**: action_dim (通常是7维)
- **参数量估算**: 
  - 假设obs_dim=50: 50×256 + 256×256 + 256×256 + 256×7 ≈ **200K参数**
  - 加上特征嵌入: +16 +32 ≈ **200K参数**

### 2. Critic网络（⚠️ 非常复杂）
- **配置**: `CRITIC_HIDDEN="256,256,256,256"` (4层，每层256)
- **代码默认**: `(512, 512, 512, 512)` (4层，每层512) ⚠️ **配置和代码不一致！**
- **复杂架构**:
  1. **状态路径**: 384维 → LayerNorm → LeakyReLU
  2. **PF特征路径**: 256维 → LayerNorm → LeakyReLU
  3. **共享底座**: 融合state+PF → 512维 → LayerNorm → LeakyReLU
  4. **每个Agent的前3维动作特征**: 128维 × 3个Agent = 384维
  5. **每个Agent的后4维PF参数特征**: 128维 × 3个Agent = 384维
  6. **双Q head结构**:
     - Q_head_1: 共享底座 + 前3维动作特征 → 3层深度处理 (512→512→512)
     - Q_head_2: 共享底座 + 后4维PF参数特征 → 3层深度处理 (512→512→512)
  7. **每个head都有残差连接**

- **参数量估算** (假设obs_dim=50, n_agents=3):
  - 状态路径: 50×3 × 384 ≈ 57K
  - PF特征路径: 3×3 × 256 ≈ 2K
  - 共享底座: (384+256) × 512 ≈ 328K
  - 动作特征提取: 3×128 × 3 + 4×128 × 3 ≈ 2.7K
  - Q_head_1: (512+384) × 512 × 3 ≈ 1.4M
  - Q_head_2: (512+384) × 512 × 3 ≈ 1.4M
  - **总计**: 约 **3.2M参数** ⚠️ **非常大！**

## 问题分析

### 1. ⚠️ 配置不一致
- **问题**: `CRITIC_HIDDEN="256,256,256,256"` 但代码默认使用 `(512, 512, 512, 512)`
- **影响**: 配置无效，实际使用512维，参数量是配置的4倍！

### 2. ⚠️ Critic网络过于复杂
- **问题**: 
  - 双Q head结构（每个head 3层×512）
  - 每个Agent都有独立的动作特征提取（128维）
  - 多层残差连接
  - 参数量约3.2M，对于强化学习任务来说过大
- **影响**:
  - 训练不稳定（梯度传播困难）
  - 容易过拟合
  - 训练速度慢
  - 内存占用大

### 3. ⚠️ Actor网络相对合理
- **参数量**: 约200K，相对合理
- **结构**: 3层×256，有残差连接，结构清晰

## 优化建议

### 1. 修复配置不一致问题
```bash
# 方案A: 修改代码默认值，使用配置的256
critic_hidden = parse_hidden_units(getattr(self.args, 'critic_hidden', None)) or (256, 256, 256, 256)

# 方案B: 修改配置，使用代码的512（不推荐，会增加复杂度）
export CRITIC_HIDDEN="512,512,512,512"
```

### 2. 简化Critic网络结构
```bash
# 方案A: 降低隐藏层维度（推荐）
export CRITIC_HIDDEN="256,256,256"  # 从4层降到3层，从512降到256

# 方案B: 简化Q head结构
# 移除双Q head，使用单Q head
# 或者减少每个head的深度（从3层降到2层）

# 方案C: 简化动作特征提取
# 从128维降到64维
```

### 3. 推荐配置
```bash
# Actor: 保持当前配置（合理）
export ACTOR_HIDDEN="256,256,256"

# Critic: 大幅简化（推荐）
export CRITIC_HIDDEN="256,256"  # 2层×256，参数量约500K（减少84%）
# 或者
export CRITIC_HIDDEN="128,128,128"  # 3层×128，参数量约300K（减少90%）
```

## 预期效果

### 简化后的参数量估算
- **当前**: Critic约3.2M参数
- **简化到256,256**: 约500K参数（减少84%）
- **简化到128,128,128**: 约300K参数（减少90%）

### 预期改进
1. ✅ 训练更稳定（梯度传播更容易）
2. ✅ 训练速度更快（参数量减少）
3. ✅ 内存占用更小
4. ✅ 不容易过拟合
5. ✅ 可能提高泛化能力

## 风险评估

### 简化可能的风险
- ⚠️ 表达能力可能下降（但当前可能已经过度）
- ⚠️ 需要重新训练（但当前训练不稳定，可能需要重启）

### 建议
- ✅ **优先修复配置不一致问题**（必须）
- ✅ **逐步简化**：先降到256,256,256，观察效果
- ✅ **如果仍然不稳定，再降到128,128,128**

