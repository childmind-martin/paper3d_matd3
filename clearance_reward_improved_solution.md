# 净空奖励改进方案：保持避障引导，避免过度保守

## 一、问题分析

### 1.1 用户担忧

**用户观点**："降低净空权重意味着算法不会趋向于躲避地形"

**正确性**：✅ 完全正确！降低权重确实会削弱避障引导。

### 1.2 当前问题

**净空奖励的影响**：
- 权重：近距离时8.0，远距离时0.2
- 累积量：近距离时8.0 × 2800 = 22,400（占正奖励累积的50.5%）
- 问题：权重过高，智能体可以通过"保持安全距离但不接近目标"来刷分

### 1.3 两难问题

1. **降低权重** → 算法不会趋向于躲避地形（用户担心）❌
2. **保持高权重** → 智能体过度关注"保持安全距离"而非"到达目标"（当前问题）❌

---

## 二、解决方案：改变计算方式，而非降低权重

### 2.1 核心思想

**不降低净空权重**，而是**改变计算方式**：
- 从"基于绝对距离"改为"基于距离变化"
- 保持避障引导：距离增加时给奖励
- 避免刷分：距离不变时不给奖励
- 惩罚接近危险：距离减少时给负奖励

### 2.2 当前计算方式（基于绝对距离）

**代码位置**：`utils/vectorized_reward_calculator.py:2760-2789`

**计算逻辑**：
```python
# 基于绝对距离的sigmoid函数
distance_above_safe = d_min_current - safe_distance
sigmoid_output = 1.0 / (1.0 + np.exp(-sigmoid_input))
clearance_reward_base = CLEARANCE_WEIGHT * sigmoid_output

# 应用动态权重
rewards = dynamic_weights * clearance_reward_base
```

**问题**：
- 只要距离>安全距离，就给正奖励（无论距离是否变化）
- 智能体可以通过"保持安全距离但不接近目标"来刷分

### 2.3 改进计算方式（基于距离变化）

**新计算逻辑**：
```python
# 1. 获取上一时刻的最小距离
if not hasattr(agent, 'last_min_distance'):
    agent.last_min_distance = d_min_current.copy()
d_min_previous = agent.last_min_distance

# 2. 计算距离变化
distance_change = d_min_current - d_min_previous

# 3. 归一化
clearance_d_max = float(os.getenv('CLEARANCE_D_MAX', '80.0'))
normalized_change = np.clip(distance_change / clearance_d_max, -1.0, 1.0)

# 4. 基于距离变化计算奖励
if distance_change > 0:
    # 距离增加：给正奖励（鼓励避障）
    clearance_reward_base = dynamic_weights * normalized_change
elif distance_change < 0:
    # 距离减少：给负奖励（惩罚接近危险，惩罚因子2.0）
    clearance_reward_base = dynamic_weights * normalized_change * 2.0
else:
    # 距离不变：给零奖励（避免刷分）
    clearance_reward_base = 0.0

# 5. 避障惩罚（距离<安全距离时）
if d_min_current < safe_distance:
    # 避障：固定高权重惩罚（无论距离目标多远）
    rewards = -PENALTY_WEIGHT * (1.0 - d_min_current / safe_distance)
else:
    # 保持安全距离：基于距离变化
    rewards = clearance_reward_base

# 6. 更新last_min_distance
agent.last_min_distance = d_min_current.copy()
```

---

## 三、效果对比

### 3.1 场景1：智能体主动避障（距离增加）

**原方式（基于绝对距离）**：
- 距离：20米（安全距离15米）
- sigmoid输出：≈ 0.9
- 近距离权重：8.0
- 奖励：8.0 × 0.9 = **7.2**（每步）
- 2800步累积：7.2 × 2800 = **20,160**

**新方式（基于距离变化）**：
- 距离变化：+5米（从15米增加到20米）
- 归一化：+5/80 = 0.0625
- 近距离权重：8.0
- 奖励：8.0 × 0.0625 = **0.5**（每步）
- **效果**：鼓励避障，但不会持续累积 ✅

### 3.2 场景2：智能体保持安全距离但不接近目标（距离不变）

**原方式（基于绝对距离）**：
- 距离：20米（保持不变）
- sigmoid输出：≈ 0.9
- 近距离权重：8.0
- 奖励：8.0 × 0.9 = **7.2**（每步）
- 2800步累积：7.2 × 2800 = **20,160**（刷分！）

**新方式（基于距离变化）**：
- 距离变化：0米（保持不变）
- 奖励：**0.0**（每步）
- **效果**：无法刷分 ✅

### 3.3 场景3：智能体接近危险（距离减少）

**原方式（基于绝对距离）**：
- 距离：12米（从15米减少到12米，但仍>安全距离）
- sigmoid输出：≈ 0.3（降低，但仍为正）
- 近距离权重：8.0
- 奖励：8.0 × 0.3 = **2.4**（每步）
- **问题**：即使接近危险，仍给正奖励 ❌

**新方式（基于距离变化）**：
- 距离变化：-3米（从15米减少到12米）
- 归一化：-3/80 = -0.0375
- 近距离权重：8.0
- 惩罚因子：2.0
- 奖励：8.0 × (-0.0375) × 2.0 = **-0.6**（每步）
- **效果**：惩罚接近危险 ✅

### 3.4 场景4：智能体距离<安全距离（需要避障）

**两种方式相同**：
- 距离：10米（安全距离15米）
- 惩罚：-4.0 × (1.0 - 10/15) = **-1.33**（每步）
- **效果**：强制避障 ✅

---

## 四、累积量对比

### 4.1 原方式（基于绝对距离）

**保持安全距离时**：
- 每步奖励：8.0 × 0.9 = 7.2
- 2800步累积：7.2 × 2800 = **20,160**

**问题**：无论是否接近目标，只要保持安全距离就能获得大量奖励。

### 4.2 新方式（基于距离变化）

**主动避障时（距离增加）**：
- 每步奖励：8.0 × 0.0625 = 0.5
- 2800步累积：0.5 × 2800 = **1,400**（假设持续避障）

**保持安全距离但不接近目标时（距离不变）**：
- 每步奖励：0.0
- 2800步累积：**0**

**接近危险时（距离减少）**：
- 每步奖励：8.0 × (-0.0375) × 2.0 = -0.6
- 2800步累积：-0.6 × 2800 = **-1,680**（假设持续接近危险）

**效果**：
- ✅ 保持避障引导：距离增加时给奖励
- ✅ 避免刷分：距离不变时不给奖励
- ✅ 惩罚接近危险：距离减少时给负奖励

---

## 五、实现建议

### 5.1 保持当前权重配置

**不降低权重**：
- `CLEARANCE_WEIGHT_NEAR = 8.0`（保持）
- `CLEARANCE_WEIGHT_FAR = 0.2`（保持）
- `CLEARANCE_PENALTY_WEIGHT = 4.0`（保持）

### 5.2 修改计算逻辑

**从"基于绝对距离"改为"基于距离变化"**：
- 使用`last_min_distance`跟踪上一时刻的距离
- 计算距离变化：`distance_change = d_min_current - d_min_previous`
- 基于距离变化计算奖励：
  - 距离增加：给正奖励
  - 距离减少：给负奖励（惩罚因子2.0）
  - 距离不变：给零奖励

### 5.3 保持避障惩罚

**距离<安全距离时**：
- 使用固定高权重惩罚（`CLEARANCE_PENALTY_WEIGHT = 4.0`）
- 无论距离目标多远，都强制避障

---

## 六、总结

### 6.1 核心改进

**不降低净空权重**，而是**改变计算方式**：
- 从"基于绝对距离"改为"基于距离变化"
- 保持避障引导：距离增加时给奖励，算法仍然趋向于躲避地形
- 避免刷分：距离不变时不给奖励，无法通过"保持安全距离但不接近目标"刷分
- 惩罚接近危险：距离减少时给负奖励，阻止接近危险

### 6.2 预期效果

1. **保持避障引导**：距离增加时给奖励，算法仍然趋向于躲避地形 ✅
2. **避免刷分**：距离不变时不给奖励，无法通过"保持安全距离但不接近目标"刷分 ✅
3. **惩罚接近危险**：距离减少时给负奖励，阻止接近危险 ✅
4. **累积量降低**：从22,400降低到约1,400（主动避障时）或0（保持安全距离时）✅

### 6.3 下一步

1. 修改`_clearance_reward_vectorized`函数，改为基于距离变化的计算
2. 保持当前权重配置（不降低）
3. 测试效果，确认避障引导仍然有效
