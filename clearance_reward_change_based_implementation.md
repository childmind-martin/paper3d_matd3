# 净空奖励改进实现：基于距离变化

## 一、修改总结

### 1.1 核心改进

**不降低净空权重**，而是**改变计算方式**：
- 从"基于绝对距离的sigmoid函数"改为"基于距离变化"
- 保持避障引导：距离增加时给奖励
- 避免刷分：距离不变时不给奖励
- 惩罚接近危险：距离减少时给负奖励（惩罚因子2.0）

### 1.2 修改位置

**文件**：`utils/vectorized_reward_calculator.py`
**函数**：`_clearance_reward_vectorized`
**行数**：2760-2789

---

## 二、新计算逻辑

### 2.1 距离变化计算

```python
# 1. 获取上一时刻的最小距离
if not hasattr(agent, 'last_min_distance'):
    agent.last_min_distance = d_min_current.copy()
d_min_previous = agent.last_min_distance

# 2. 计算距离变化
distance_change = d_min_current - d_min_previous

# 3. 归一化
clearance_d_max = 80.0
normalized_change = np.clip(distance_change / clearance_d_max, -1.0, 1.0)
```

### 2.2 基于距离变化的奖励

```python
# 距离增加：给正奖励（鼓励避障）
if distance_change > 1e-6:
    clearance_reward = dynamic_weights * normalized_change

# 距离减少：给负奖励（惩罚接近危险，惩罚因子2.0）
elif distance_change < -1e-6:
    clearance_reward = dynamic_weights * normalized_change * 2.0

# 距离不变：给零奖励（避免刷分）
else:
    clearance_reward = 0.0
```

### 2.3 避障惩罚（保持不变）

```python
# 距离<安全距离时：固定高权重惩罚
if d_min_current < safe_distance:
    rewards = -PENALTY_WEIGHT * (1.0 - d_min_current / safe_distance)
else:
    rewards = clearance_reward_base
```

---

## 三、效果对比

### 3.1 场景1：智能体主动避障（距离增加）

**原方式（基于绝对距离）**：
- 距离：20米（安全距离15米）
- sigmoid输出：≈ 0.9
- 奖励：8.0 × 0.9 = **7.2**（每步）
- 2800步累积：**20,160**

**新方式（基于距离变化）**：
- 距离变化：+5米（从15米增加到20米）
- 归一化：+5/80 = 0.0625
- 奖励：8.0 × 0.0625 = **0.5**（每步）
- **效果**：鼓励避障，但不会持续累积 ✅

### 3.2 场景2：智能体保持安全距离但不接近目标（距离不变）

**原方式（基于绝对距离）**：
- 距离：20米（保持不变）
- sigmoid输出：≈ 0.9
- 奖励：8.0 × 0.9 = **7.2**（每步）
- 2800步累积：**20,160**（刷分！）

**新方式（基于距离变化）**：
- 距离变化：0米（保持不变）
- 奖励：**0.0**（每步）
- **效果**：无法刷分 ✅

### 3.3 场景3：智能体接近危险（距离减少）

**原方式（基于绝对距离）**：
- 距离：12米（从15米减少到12米）
- sigmoid输出：≈ 0.3（降低，但仍为正）
- 奖励：8.0 × 0.3 = **2.4**（每步）
- **问题**：即使接近危险，仍给正奖励 ❌

**新方式（基于距离变化）**：
- 距离变化：-3米（从15米减少到12米）
- 归一化：-3/80 = -0.0375
- 惩罚因子：2.0
- 奖励：8.0 × (-0.0375) × 2.0 = **-0.6**（每步）
- **效果**：惩罚接近危险 ✅

---

## 四、累积量对比

### 4.1 原方式（基于绝对距离）

**保持安全距离时**：
- 每步奖励：8.0 × 0.9 = 7.2
- 2800步累积：7.2 × 2800 = **20,160**

**问题**：无论是否接近目标，只要保持安全距离就能获得大量奖励。

### 4.2 新方式（基于距离变化）

**主动避障时（距离增加）**：
- 每步奖励：8.0 × 0.0625 = 0.5
- 2800步累积：0.5 × 2800 = **1,400**（假设持续避障）

**保持安全距离但不接近目标时（距离不变）**：
- 每步奖励：0.0
- 2800步累积：**0**

**接近危险时（距离减少）**：
- 每步奖励：8.0 × (-0.0375) × 2.0 = -0.6
- 2800步累积：-0.6 × 2800 = **-1,680**（假设持续接近危险）

**效果**：
- ✅ 保持避障引导：距离增加时给奖励
- ✅ 避免刷分：距离不变时不给奖励
- ✅ 惩罚接近危险：距离减少时给负奖励
- ✅ 累积量大幅降低：从20,160降低到约1,400或0

---

## 五、权重配置（保持不变）

### 5.1 当前配置

```bash
export CLEARANCE_WEIGHT=${CLEARANCE_WEIGHT:-8.0}          # 基础权重（保持）
export CLEARANCE_WEIGHT_NEAR=${CLEARANCE_WEIGHT_NEAR:-8.0}  # 近距离权重（保持）
export CLEARANCE_WEIGHT_FAR=${CLEARANCE_WEIGHT_FAR:-0.2}   # 远距离权重（保持）
export CLEARANCE_PENALTY_WEIGHT=${CLEARANCE_PENALTY_WEIGHT:-4.0}  # 避障惩罚权重（保持）
```

### 5.2 不降低权重的原因

**用户担忧**："降低净空权重意味着算法不会趋向于躲避地形"

**解决方案**：
- ✅ 不降低权重（保持8.0）
- ✅ 改变计算方式（从绝对距离改为距离变化）
- ✅ 保持避障引导：距离增加时给奖励
- ✅ 避免刷分：距离不变时不给奖励

---

## 六、总结

### 6.1 核心改进

**不降低净空权重**，而是**改变计算方式**：
- 从"基于绝对距离"改为"基于距离变化"
- 保持避障引导：距离增加时给奖励，算法仍然趋向于躲避地形
- 避免刷分：距离不变时不给奖励，无法通过"保持安全距离但不接近目标"刷分
- 惩罚接近危险：距离减少时给负奖励，阻止接近危险

### 6.2 预期效果

1. **保持避障引导**：距离增加时给奖励，算法仍然趋向于躲避地形 ✅
2. **避免刷分**：距离不变时不给奖励，累积量从20,160降低到0 ✅
3. **惩罚接近危险**：距离减少时给负奖励，阻止接近危险 ✅
4. **累积量降低**：从20,160降低到约1,400（主动避障时）或0（保持安全距离时）✅

### 6.3 下一步

1. ✅ 已修改`_clearance_reward_vectorized`函数，改为基于距离变化的计算
2. ✅ 保持当前权重配置（不降低）
3. ⏳ 测试效果，确认避障引导仍然有效
